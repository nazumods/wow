# WoW AddOn Suite — Code Context Index

> **Purpose:** Top-level map of the Nazuraki addon suite. Each addon's full code
> reference now lives in its own `<addon>/CONTEXT.md` — load only the ones you need.
> This root file holds the cross-cutting bits: the dependency graph, a one-line
> summary + pointer per addon, and the global slash command registry.

---

## Runtime Environment

WoW runs **Lua 5.1**. All addon code must be compatible with Lua 5.1 — in particular:

- No `goto` / `::label::` (added in Lua 5.2) — use `if/end` blocks to skip loop iterations
- No integer division operator `//` (added in Lua 5.3) — use `math.floor(a / b)`
- No bitwise operators `&`, `|`, `~`, `<<`, `>>` (added in Lua 5.3) — use `bit.band` etc. (WoW provides the `bit` library)
- No `table.move`, `table.unpack` (5.2+) — use `unpack()`

---

## Dependency Graph (All Addons)

```
LibNAddOn
    |
    +-- LibNUI ──────────────────────────→ LibNUI_Test (LoadOnDemand)
    |     |
    |     +-- LibNUI-ModelViewer  (LibNAddOn + LibNUI; registers ui.Model; consumed by Warbandeer_Collected + LibNUI_Test)
    |     +-- ShadowsOfUI-XP
    |     +-- ShadowsOfUI-GCD
    |     +-- ShadowsOfUI-Castbar
    |     +-- ShadowsOfUI-Compartment
    |     +-- HideStanceBar
    |     +-- Warbandeer_Alias
    |     +-- Recycle
    |     +-- Warbandeer_Decor  (LibNAddOn + LibNUI; populates WarbandeerHousingDecorApi for Warbandeer; no Warbandeer_Characters — decor is account-wide)
    |     +-- Warbandeer_Characters  (populates WarbandeerApi)
    |           |
    |           +-- Warbandeer            (optionally reads WarbandeerBarsApi + WarbandeerCollectedApi + WarbandeerHousingDecorApi + ShadowsOfUI_UpgradeApi)
    |           +-- Warbandeer_Collected  (also populates WarbandeerCollectedApi for Warbandeer)
    |           +-- ShadowsOfUI-Upgrade   (publishes ShadowsOfUI_UpgradeApi; consumed by Warbandeer)
    |
    +-- Warbandeer_Bars      (LibNAddOn only — headless data layer, populates WarbandeerBarsApi)
    +-- CombatOutline    (LibNAddOn only, no LibNUI)
    +-- ShadowsOfUI-DMF (LibNAddOn only, no LibNUI)
    +-- ShadowsOfUI-Ilvl (LibNAddOn only, no LibNUI; optional Baganator + Bagnon/Bagnonium integration)
    +-- ShadowsOfUI-Known (LibNAddOn + Warbandeer_Characters; optional Warbandeer; no LibNUI)
    +-- ShadowsOfUI-Artisan (LibNAddOn + Warbandeer_Characters; optional Warbandeer; no LibNUI)
    +-- ShadowsOfUI-Inventory (LibNAddOn + Warbandeer_Characters; no LibNUI)
    +-- ShadowsOfUI-Reputations (LibNAddOn + Warbandeer_Characters; no LibNUI)
    +-- ShadowsOfUI-Quests (LibNAddOn + Warbandeer_Characters; no LibNUI)
    +-- ShadowsOfUI-Delves (LibNAddOn + LibNUI + Warbandeer_Characters; LibNUI only for the /sdelves copy window)
    +-- ShadowsOfUI-Collectibles (LibNAddOn; optional Warbandeer_Characters for cross-alt recipes; optionally reads HousingDecorApi for decor owned-state; no LibNUI)
    +-- ShadowsOfUI-HousingVendor (LibNAddOn only; overlays decor on the merchant + bags/bank/Bagnon; populates HousingDecorApi; no LibNUI)
    +-- ShadowsOfUI-ProfCommissions (LibNAddOn only; hooks Blizzard_Professions crafting-orders list; no LibNUI)
    +-- ShadowsOfUI-QuestXP (LibNAddOn only; hooks the quest log's Details-pane rewards; no LibNUI)
    +-- ShadowsOfUI-PostOffice (LibNAddOn + LibNUI; optional Warbandeer_Characters for the alts menu; augments native mail frames)
    +-- MouselookToggle (LibNAddOn only, no LibNUI)

(no LibN dependency):
    HideBagBar       (raw WoW API only)
    BarNonce         (raw WoW API only)
```

---

## Addon Index

Load the linked `CONTEXT.md` for full file maps, class hierarchies, API surfaces, and data structures.

| Addon | Summary | Reference |
|---|---|---|
| **LibNAddOn** | Bootstrapping factory (`LibNAddOn(features)`), class system, lua utils, event/DB/settings wiring. Every addon depends on it. | [LibNAddOn/CONTEXT.md](LibNAddOn/CONTEXT.md) |
| **LibNUI** | OOP UI widget library; global `LibNUI` / `ns.ui`. Region→Frame hierarchy: Texture, Label, StatusBar, Button, TableFrame, TitleFrame, TabFrame, Tooltip, CopyWindow, settings widgets. Themable via `ui.Theme{}` / `ui.themes.dark`. Shared copy window `ui.ShowCopyWindow` + `/wdebug`; `LibNUIDB` (v1). | [LibNUI/CONTEXT.md](LibNUI/CONTEXT.md) |
| **LibNUI-ModelViewer** | `ModelScene`-backed 3D model-viewer widget, extracted from core LibNUI (#591). Registers `Model` back onto the shared LibNUI table (`ui.Model` / `LibNUI.Model`) via `X-NUI-UI: LibNUI`; consumers add it to `## Dependencies` so the widget exists at file-load time. No DB, no commands. Consumed by Warbandeer_Collected (dressing room + the outfit library's preview pane) + LibNUI_Test. | [LibNUI-ModelViewer/CONTEXT.md](LibNUI-ModelViewer/CONTEXT.md) |
| **Warbandeer_Characters** | Data collection backbone; populates `WarbandeerApi`. Broker system, per-character struct, account-wide warband wealth + bank prof-gear cache (warband/character/guild) + equippable-gear cache (bags + warband/personal banks) + per-character world-quest gear-reward cache + per-slot empty-socket counts + per-character secondary-stats snapshot + per-character bag/bank item-count index (warband-stock tooltip) + per-character mail cache (expiry warnings) + per-character reputation cache (cross-alt standings) + per-character owned-auction cache (Summary column) + per-character quest log (active set + completed bitmap) + per-character weekly Delver's Bounty claim + per-character delve completion-time cache (personal run-time averages). Settings panel **Warbandeer → Logging** subcategory: opt-in combat-logging toggle (`logging.lua`). Per-character playtime now also tracked **per local calendar day** (`playtime.byDay`, wall-clock session accounting). Per-character `guid` (UnitGUID) now stamped each login. Per-character applied cosmetic appearance-glyph cache (`glyphs.applied`, per spec) + learned class-unlock cache (`glyphs.unlocks` — Druid Tome of the Wilds, Hunter Tomes & Tames) + the static catalogs (`AppearanceGlyphs`/`AppearanceUnlocks`/`LearnedUnlocks`/`ChallengeTames`). Per-character Hunter pet roster (`pets` — active Call-Pet slots + stabled pets w/ creatureID/displayID, last-seen at a stable master), cross-referenced against the curated rare-tame catalog (`ChallengeTames`) into an owned/missing checklist (`GetChallengeTames`). Per-character player titles (`titles` — the `known` earned list + featured `current` id + `currentName`, last-seen), surfacing each character's current title as Warbandeer's Summary **Titles** column. Per-character **weekly profession knowledge** (`professionKnowledge` — treatise / weekly-quest / treasure / gathering knowledge-point progress per Midnight profession, read from hidden quest flags and cleared each weekly reset; retires the standalone WeeklyKnowledge addon). Per-character **neighborhood endeavor** (`housing.active` — which endeavor a character is feeding) plus an account-wide `db.housing` store (per-neighborhood faction + endeavor title/progress/reset + per-house level/favor), surfacing Warbandeer's Summary **Endeavors** column + Overview **Houses** strip (#550). `WarbandeerCharDB` (v41). | [Warbandeer_Characters/CONTEXT.md](Warbandeer_Characters/CONTEXT.md) |
| **Warbandeer** | Main viewer UI (`/warband`, `/wb`). 18 views (16 without the optional Collected/Decor addons), MainWindow, faction widget, `profIntent`, Detail consumables box (ClassCodex via ShadowsOfUI-Upgrade), Titles view (optional live Epithet read), Endeavors Summary column + Overview Houses strip (#550), `WarbandeerDB` (v8). | [Warbandeer/CONTEXT.md](Warbandeer/CONTEXT.md) |
| **Warbandeer_Alias** | Guild-chat alias prefix hook. Single file; `Warbandeer_AliasDB` (v1). | [Warbandeer_Alias/CONTEXT.md](Warbandeer_Alias/CONTEXT.md) |
| **Warbandeer_Collected** | Transmog set tracker (`/collected`, `/collect`). DataView grid, scan logic, per-set **wanted** flag + **S–F tier ranking** (global baseline w/ per-race overrides) edited from the grid (shift-click) + dressing room, plus a **live/PTR toggle** previewing the upcoming (PTR-only) sets (`data/sets_ptr.lua`, generated by `tools/update-sets.ps1 -PtrDelta`) and an **Armor/Weapons** grid toggle browsing weapons by source × weapon type (`data/weaponsources.lua`, generated by `-Weapons`) with a difficulty-labelled dressing-room drill-in — weapons carrying the same **wanted** flag + **S–F tier** per appearance, edited from the room's own ratings row — which follows `_focus`, so it rates the browsed weapon look while one is on the doll and the previewed set otherwise (#827) — plus a shift-click on any chooser row, and surfaced in the grid as a cell ★/pip, a wanted-only filter and a header tally, `WarbandeerCollectedDB` (v10; on top of v4's `dressPos`/`windowPos`: v5's `weaponWanted` per-appearance list, v6's `cosmeticWanted`/`illusionWanted`, v7's `lastOutfit`, v8's account-wide `outfits` library, v9's `libraryPos` for the outfit library window, and v10's `weaponRank`); exposes `WarbandeerCollectedApi` (read scan data + PTR delta + read/write ratings; consumed by Warbandeer's `collected` view). | [Warbandeer_Collected/CONTEXT.md](Warbandeer_Collected/CONTEXT.md) |
| **Warbandeer_Decor** | Housing decor collection tracker (`/housingdecor`, `/wbdecor`). Account-wide (decor collection is warband-level, so no per-character axis and no `Warbandeer_Characters` dep). A single filterable **windowed** catalog list (~1800 entries, so only visible rows are live) — one row per decor entry (quality icon/name, owned count, first-acquisition-bonus star, **wanted** star) — enumerated live from `C_HousingCatalog` (searcher `RunSearch` → `GetCatalogSearchResults` → `NormalizeEntry`; never persisted), filtered by category/subcategory/unowned/wanted/search; per-decor **wanted** flag (shift-click, keyed by `recordID`). `WarbandeerHousingDecorDB` (v1: `wanted` + cached counts + `windowPos`); exposes `WarbandeerHousingDecorApi` (scan data + read/write wanted + the embeddable `List`; consumed by Warbandeer's optional `decor` view). | [Warbandeer_Decor/CONTEXT.md](Warbandeer_Decor/CONTEXT.md) |
| **ShadowsOfUI-XP** | Minimal full-width XP bar at screen bottom (below max level only). Single file, no DB. | [ShadowsOfUI-XP/CONTEXT.md](ShadowsOfUI-XP/CONTEXT.md) |
| **ShadowsOfUI-Compartment** | Makes Blizzard's `AddonCompartmentFrame` (minimap addon-count button) movable via ALT+left-drag and swaps its count number for a custom icon. Settings toggle + `/scompartment` (`reset`); `ShadowsOfUI_CompartmentDB` (v1). | [ShadowsOfUI-Compartment/CONTEXT.md](ShadowsOfUI-Compartment/CONTEXT.md) |
| **HideStanceBar** | Hides the stance bar via reparenting, per-class toggles. `HideStanceBarDB` (v1). | [HideStanceBar/CONTEXT.md](HideStanceBar/CONTEXT.md) |
| **HideBagBar** | Hides backpack/bag slot buttons. Raw WoW API only — no LibNAddOn. | [HideBagBar/CONTEXT.md](HideBagBar/CONTEXT.md) |
| **ShadowsOfUI-GCD** | Minimal GCD sweep bar anchored between the primary and secondary resource bars. Single file, no DB, no slash commands. | — |
| **ShadowsOfUI-Castbar** | Three movable, cosmetic cast bars (target + focus + player; player off by default) on LibNUI `StatusBar` — spell icon/name/time, non-interruptible greyed, channels green. Text size 8–22px; placed via Blizzard Edit Mode (enter/exit hooks → draggable sample with a hand-rolled selection box; click a bar for a LibNUI config popup — enable/text size/reset position). Reads `UnitCastingInfo`/`UnitChannelInfo` only (no secret data). `/scast`; `ShadowsOfUI_CastbarDB` (v3). | [ShadowsOfUI-Castbar/CONTEXT.md](ShadowsOfUI-Castbar/CONTEXT.md) |
| **BarNonce** | Sets 70% opacity on Action Bars 1 and 2. Raw WoW API only — no LibNAddOn. Single file, no DB, no slash commands. | — |
| **MouselookToggle** | Keybind (Options → Keybindings → Shadows of UI, via `Bindings.xml`) that toggles mouselook. While toggled, `CursorFreelookCentering`/`CursorCenteredYPos` CVars (via `SetTemporaryCVar`, armed one frame after `MouselookStart` — 10.2 camera-jolt bug) center the hidden cursor at a configurable height, action-combat style; held right-click freelook keeps the stock frozen cursor. Crosshair reticle (`crosshair_unablecrosshairs_48` atlas) tracks `GetCursorPosition` — where `@cursor` macros target — fading in immediately when toggled, debounced 0.3s during held freelook (optional). Settings: reticle height %, show-during-held. `MouselookToggleDB` (v1); no slash commands. | [MouselookToggle/CONTEXT.md](MouselookToggle/CONTEXT.md) |
| **CombatOutline** | Toggles `OutlineEngineMode` CVar in/out of combat. Single file. | [CombatOutline/CONTEXT.md](CombatOutline/CONTEXT.md) |
| **Recycle** | Auto-sells grey + marked items at merchants (`/recycle`). Per-character `RecycleDB` (v1). | [Recycle/CONTEXT.md](Recycle/CONTEXT.md) |
| **ShadowsOfUI-DMF** | Headless Darkmoon Faire helper: merchant material auto-buy + waypoint/map-pin guidance to profession quest givers. `/sdmf` dev command. No UI, no DB. | [ShadowsOfUI-DMF/CONTEXT.md](ShadowsOfUI-DMF/CONTEXT.md) |
| **ShadowsOfUI-Ilvl** | Overlay: item level + compact upgrade-track code (`A/V/C/H/M`+rank, e.g. `C2`) on gear icons (bags/bank/loot/guild bank/Baganator/Bagnon), inset-or-overlay per panel on the character/inspect paperdolls, plus the inspected player's average ilvl atop the inspect model. Per-place toggles + min-quality select via Settings panel; `ShadowsOfUI_IlvlDB` (v3). `/silvl` dev dump. | [ShadowsOfUI-Ilvl/CONTEXT.md](ShadowsOfUI-Ilvl/CONTEXT.md) |
| **ShadowsOfUI-Known** | Headless tooltip addon: on recipe item tooltips, a "Learnable by:" block (alts with the profession that haven't learned it; red if skill too low) plus a "Known by:" line naming alts who already know it (by recipe name); and on the **Place Crafting Order** browse list, a "Known by:" / red "Not Known" line (matched by exact captured recipe id) plus a "Guild crafters:" line — guild members who can craft it — via Blizzard's **native async** guild query (`C_GuildInfo.QueryGuildMembersForRecipe`, no comms; replaces VamoosesGuildCraft). Reads `WarbandeerApi` + optional `WarbandeerDB.profIntent`. `/sknown` dev dumps. No UI, no DB. | [ShadowsOfUI-Known/CONTEXT.md](ShadowsOfUI-Known/CONTEXT.md) |
| **ShadowsOfUI-HousingVendor** | Overlays housing-decor icons with an in-storage owned-count badge (bottom-left), a gold first-acquisition-bonus star on unowned bonus decor (top-left), and an optional owned-check (top-left, off by default) — on the **Merchant** window plus **bags / bank / Bagnon** (each a per-surface toggle; merchant always on). One synchronous `C_HousingCatalog.GetCatalogEntryInfoByItem` call per item, class-gated + session-cached in `decor.lua` and shared via **`HousingDecorApi`** (`X-NUI-API`); a `HOUSING_STORAGE_UPDATED` wipe + all-surface refresh + login catalog prime. Native replacement for the at-vendor decor overlays in Ludius Plus / Plumber / RiddleCompass / Decor Vendor; complements (and now feeds decor owned-state to) ShadowsOfUI-Collectibles. `/shvendor` (`itemtest` dev dump). Standalone, no LibNUI. `ShadowsOfUI_HousingVendorDB` (v2). | [ShadowsOfUI-HousingVendor/CONTEXT.md](ShadowsOfUI-HousingVendor/CONTEXT.md) |
| **ShadowsOfUI-ProfCommissions** | Two additions to the crafter **Crafting Orders** browse list: (1) replaces Blizzard's generic reward treasure-chest icon in the Commission column with the order's **actual reward item/currency icons** (quality-bordered, count-stamped, hoverable, and **gold-glowed** at Epic quality or better — items *and* currency, so a Gold Star-class reward can't be scrolled past) — Patron-order rewards legible at a glance; (2) **replaces the text "Reagents" column** with an **"Info" column** of two status icons — **first-craft bonus** (`IsRecipeFirstCraft`; a red ⊗ replaces it when the recipe is **unlearned**) + **reagent provision** (All/Some/None). Hooks `ProfessionsCrafterTableCellCommissionMixin:Populate` + `OrdersPage:SetupTable` (repurposes the Reagents column with a `column.xml` cell template). `/sprofcomm` (status; `size <n>` dev tuning). No LibNUI, no DB. | [ShadowsOfUI-ProfCommissions/CONTEXT.md](ShadowsOfUI-ProfCommissions/CONTEXT.md) |
| **ShadowsOfUI-Collectibles** | Tints already-known / still-collectible items on the Merchant and Auction House browse list (recipes, toys, mounts, pets, caged battlepets, housing decor, plus tracked quest/special/container items; transmog/cosmetics out of scope; guild bank not yet supported — reworked in 12.0). Recipes use **cross-alt** knowledge (`WarbandeerApi` — known if any alt has learned it, with a current-char tooltip fallback); everything else uses live account/collection APIs. Settings panel (known-item tint picked via a wrap-arrow stepper atop a live control-vs-tinted vendor-card preview — 7 bold/legible presets, blue by default, no dropdown — plus preview saturation/dim sliders + monochrome + per-surface toggles), `ShadowsOfUI_CollectiblesDB` (v2). `/scollect` (custom colour picker + itemtest). Replaces the standalone AlreadyKnown addon. | [ShadowsOfUI-Collectibles/CONTEXT.md](ShadowsOfUI-Collectibles/CONTEXT.md) |
| **ShadowsOfUI-Quests** | Headless addon: adds an "Also on this quest / Completed by" cross-alt block to the world-map quest-log tooltip (`EventRegistry "QuestMapLogTitleButton.OnEnter"` hook). Renders `WarbandeerApi:GetQuestStatus` (data from Warbandeer_Characters' new `questlog` broker — active set + 32-bit completed-quest bitmap). `/squests <questID>` dev/lookup. No UI, no DB. | [ShadowsOfUI-Quests/CONTEXT.md](ShadowsOfUI-Quests/CONTEXT.md) |
| **ShadowsOfUI-QuestXP** | Headless addon: appends a `(X%)` tag to the XP reward shown in the map quest log's Details pane — percentage of the player's current-level XP requirement the quest's reward is worth. Hooks `QuestInfo_ShowRewards`, gated to `MapQuestInfoRewardsFrame` (map Details pane only, not NPC quest dialogs). `/squestxp` dev/lookup. No UI, no DB. | [ShadowsOfUI-QuestXP/CONTEXT.md](ShadowsOfUI-QuestXP/CONTEXT.md) |
| **ShadowsOfUI-PostOffice** | Headless mailbox helper re-derived from Postal's pure-logic modules: **Rake** (reports coin collected per visit), **TradeBlock** (raises the `BlockTrades` CVar via `SetTemporaryCVar` while the mailbox is open), **Wire** (fills a blank `SendMailSubjectEditBox` with the coin amount via a lazy `SendMailMoney` value-changed hook). **Express** (modifier-click: ctrl-click an inbox letter to return it via a taint-safe `InboxFrame_OnClick` wrapper; alt-click a bag item to attach it to the open letter via a `HandleModifiedItemClick` posthook, optional auto-send). **Forward** (button on `OpenMailFrame` that re-sends a letter's text + attachments — stackables included — shuttling items letter→bag→outgoing, locating each by itemID+count and `SplitContainerItem`). **CarbonCopy** (button that copies a letter's text + AH-invoice breakdown into LibNUI's `ShowCopyWindow`). **BlackBook** (recipient menu on the "To:" field — recently mailed + warband alts via optional `WarbandeerApi` + friends + guild, class-coloured `MenuUtil` menu — plus inline `OnChar` autocomplete from the same lists; Blizzard's dead autocomplete popup suppressed). **QuickAttach** (column of trade-goods category buttons off `MailFrame`'s right edge — left-click bulk-attaches every matching non-soulbound stack from bags via whole-stack `PickupContainerItem` + `ClickSendMailItemButton`). **DoNotWant** (per-row return/delete icon on each inbox letter — first consumer of the shared `OnInboxRow` decorator; click to return or delete, confirm popups for item/coin loss). **Select** (checkbox per inbox row + Open/Return buttons — owns the row re-layout that DoNotWant's icon rides along; plain/shift-range/ctrl-same-sender multi-select; batch open/return via a highest-index-first throttled state machine). Mailbox lifecycle via `PLAYER_INTERACTION_MANAGER_FRAME_SHOW/HIDE` filtered to `MailInfo`; refresh dispatches for the open letter (`OpenMail_Update`) and the inbox rows (shared `InboxFrame_Update` → `OnInboxRow`). Per-feature Settings toggles; `/spost`; `ShadowsOfUI_PostOfficeDB` (v7). LibNAddOn + LibNUI (copy window) + optional Warbandeer_Characters (alts). | [ShadowsOfUI-PostOffice/CONTEXT.md](ShadowsOfUI-PostOffice/CONTEXT.md) |
| **ShadowsOfUI-Delves** | Headless tooltip addon: appends the current character's average delve completion time (Tier 11 at max level, else an all-tiers average) to a delve's map entrance-pin tooltip (`AreaPOIPinMixin:OnMouseEnter` hook, gated via `C_AreaPoiInfo.GetDelvesForMap`). Renders `WarbandeerApi:GetDelveStats` (run-times tracked + stored by Warbandeer_Characters' `delvetimes` event tracker). `/sdelves` dev/calibration (output opens in LibNUI's shared copy window). LibNUI only for that window; no DB. | [ShadowsOfUI-Delves/CONTEXT.md](ShadowsOfUI-Delves/CONTEXT.md) |
| **ShadowsOfUI-Reputations** | Headless addon: shows each character's faction standings warband-wide in two places — the in-game Reputation tab (hover a faction, via a `ReputationEntryMixin:OnEnter` hook) and faction-tied item tooltips (name-matched). Renders `WarbandeerApi:GetFactionStandings` (data from Warbandeer_Characters' new `reputations` broker). `/sreps <factionID\|name>` dev/lookup. No UI, no DB. | [ShadowsOfUI-Reputations/CONTEXT.md](ShadowsOfUI-Reputations/CONTEXT.md) |
| **ShadowsOfUI-Inventory** | Headless tooltip addon: adds a "Warband Inventory" block to item tooltips — per-character counts (bags + personal bank), the shared warband bank, and guild banks, with a grand total. Shift hides it. Reads `WarbandeerApi:GetItemCounts` (counting lives in Warbandeer_Characters' new `inventory` broker + `bank` item maps). `/swinv <itemID\|link>` dev/find dump. No UI, no DB. | [ShadowsOfUI-Inventory/CONTEXT.md](ShadowsOfUI-Inventory/CONTEXT.md) |
| **ShadowsOfUI-Artisan** | Adds a badge — the current-expansion artisan crafting currency (Midnight's per-profession "Artisan's … Moxie", gathering profs included) for the logged-in character — to the crafting window (`ProfessionsFrame`, beside the Concentration readout), its Crafting Orders tab (`OrdersPage` header) and the spellbook professions page (`ProfessionsBookFrame`, under each profession's spell labels — plus an uncaptured-detail **⚠** beside a primary profession (left of its spell buttons) when Warbandeer hasn't recorded its recipes), plus an account-wide hover breakdown across alts that have the profession. Reads `WarbandeerApi` (+ optional `WarbandeerDB.profIntent`); pairs with a new `artisanCurrency` broker in Warbandeer_Characters that caches the per-character amount. `/sartisan` dev dump. No UI lib, no DB. | [ShadowsOfUI-Artisan/CONTEXT.md](ShadowsOfUI-Artisan/CONTEXT.md) |
| **ShadowsOfUI-Upgrade** | Headless gear-upgrade finder + tooltip addon: ilvl-gated upgrades per character (bags/bank/warband bank, active world quests, bundled faction-quartermaster gear), stat-tagged from small built-in tables (spec stat-priority + quartermaster gear; standalone). Also flags **missing enchants + empty gem sockets** and recommends the enchant/gem to apply (ClassCodex per-spec when installed, else a bundled fallback). Reads `WarbandeerApi` (+ optional `ClassCodexGearData`), publishes `ShadowsOfUI_UpgradeApi` (consumed by Warbandeer's Summary/Gear/Detail views). `/supgrade` dev dump. No UI, no DB. | [ShadowsOfUI-Upgrade/CONTEXT.md](ShadowsOfUI-Upgrade/CONTEXT.md) |
| **Warbandeer_Bars** | Headless action-bar/keybind/macro profile layer per char+spec; `WarbandeerBarsApi`. `WarbandeerBarsDB` (v2). | [Warbandeer_Bars/CONTEXT.md](Warbandeer_Bars/CONTEXT.md) |

LibNUI_Test is a LoadOnDemand visual test harness for LibNUI (`/nui test [key]`); it has no standalone reference file.

---

## Apps (`apps/` — not addons, excluded from the release pipeline)

| App | Summary | Reference |
|---|---|---|
| **warbandeer-desktop** | Tauri 2 + Svelte 5 desktop companion: reads SavedVariables (mlua) + combat logs, mirrors the Overview view, plus an operator-only Ops tab that manages the `warbandeer-discord` bot over SSH (backend shared with wow-companion via `apps/bot-ops`; the privileged surface is the box's `ops/bot-ops.sh`). Embeds a generated wago.tools lookup bundle (`src-tauri/data/static-data.json` — currencies, plus the achievements Warbandeer tracks) so id-only saved data renders offline; refreshed weekly by `update-static-data.yml`. Released via `app-release.yml` (`app-*` tags). | [apps/warbandeer-desktop/CONTEXT.md](apps/warbandeer-desktop/CONTEXT.md) |
| **warbandeer-discord** | Bun + TypeScript Discord bot: DMF/reset timers, weekly-reset + realm-up announcements (Blizzard API), GitHub release notifications, admin-gated `/update` self-restart. | [apps/warbandeer-discord/CONTEXT.md](apps/warbandeer-discord/CONTEXT.md) |
| **bot-ops** | Not an app — the shared module behind the operator-only Bot Ops panel, extracted because **two** apps ship it: `warbandeer-desktop` here (cargo `path` dep + `@bot-ops` vite alias) and `roshne/wow-companion` (vendored via its `npm run vendor:bot-ops`). Holds the Tauri commands, the SSH plumbing, the wire types and the editable-key whitelist; **not** the panel UI, since the two apps render Svelte and React. This repo is the source of truth — changes start here and flow outward. | [apps/bot-ops/CONTEXT.md](apps/bot-ops/CONTEXT.md) |

---

## External References (separate repos, not part of this repo)

Clone these alongside this repo when the work needs them; the paths are yours to choose.

| Repo | What it is | When to reach for it |
|---|---|---|
| [**battlenet-api-research**](https://github.com/roshne/battlenet-api-research) (private) | Offline, diff-able capture of the Battle.net → WoW **Web REST API** — the Web-API equivalent of what `wow-ui-source` is for addons. Three layers: browsable docs (`docs/INDEX.md`), a generated OpenAPI 3.1 spec (`openapi/battlenet-wow.openapi.json`, 217 paths), captured 200-response schemas per endpoint (`openapi/responses/*.schema.json`), and a typed TS client (`client/`). Downstream consumer: **wow-companion** below (Tauri app — client secret in the OS keychain, token exchange in Rust, calls proxied so the webview never sees the secret). | Any "does the Web API expose X / what does it return" question — `jq` the spec and the response schemas rather than re-deriving from third-party client libraries or the JS-rendered dev portal. Two gotchas worth knowing up front: **character**-scoped `/profile/wow/character/...` endpoints work on client credentials alone (only `/profile/user/wow/*` needs user OAuth), and there is **no currency endpoint** — currency name/icon metadata comes from wago.tools `CurrencyTypes` instead. |
| [**wow-companion**](https://github.com/roshne/wow-companion) | Tauri 2 + React/TS desktop app — the downstream consumer of `battlenet-api-research` (vendored typed client in `src/vendor/battlenet-wow-client/`). Renders the Web API: WoW Token price, realm status, character lookup (overview, gear paper doll + gear check, spec, M+, PvP, professions, reputations, collections, raids, achievements), guild roster, auctions. Also reads **this suite's** `Warbandeer_Characters` SavedVariables locally (mlua) for its Warband tab + gear board, and manages `apps/warbandeer-discord` over SSH from a hidden Bot Ops tab. Client secret lives in the OS keychain (`keyring`) with the token exchange in Rust, so the webview never sees it. | The reference implementation for anything Web-API-backed, and the precedent for a **credentialed** desktop consumer — contrast `apps/warbandeer-desktop`, which is deliberately offline and credential-free. Worth checking before designing a new out-of-game view: it may already render it. |
| [**wow-ui-source**](https://github.com/Gethe/wow-ui-source) | Gethe's mirror of Blizzard's extracted retail UI source — `Interface/AddOns/Blizzard_*` Lua/XML plus `Blizzard_APIDocumentationGenerated`. On the `live` branch, pinned to build `12.0.7.68453`. | Atlas names, Blizzard frame/mixin internals, and the real call patterns behind a `C_*` API. Grep your local clone with the Grep tool's `path=` rather than guessing — but prefer an Explore agent for open-ended "how does Blizzard do X" questions, since these files are large. |

---

## Slash Command Registry

| Addon | Commands | Sub-commands |
|---|---|---|
| LibNAddOn | `/lib`, `/rl`, `/fs`, `/etc` | `player`; `/rl` (ReloadUI), `/fs` (framestack toggle), `/etc` (Edit Mode) — suite-wide, unscoped |
| LibNUI | `/nui`, `/wdebug` | `version`, `test [key]`; `/wdebug <lua>` (raw command: eval Lua → copyable window) |
| Warbandeer_Characters | `/characters`, `/wbc` | `list`, `delete <name>`, `cleanup`, `refresh`, `refresh items/locks`, `dump`, `dump bank/bankgear/gt/locks/artifact/warband/profgear/wq[ self]/m+/vault/delves/dungeons/glyphs/pets/demons/tames/titles/travelerslog/knowledge`, `wdump [*]` / `wdump <same subcmds>` (copyable-window variant of every `dump`), `clear delves/dungeons`, `stat`, `missing`, `missing me`, `missing audit`, `wmissing` |
| Warbandeer | `/warband`, `/wb` | `""` (open), `overview`, `summary`, `vault`, `gear`, `detail`, `roles`, `races`, `legion`, `midnight`, `profs`, `midnightprofs`, `crafting`, `playtime`, `bars`, `collected`, `reputations`, `titles`, `check legion`, `enchants` (list accepted wrong-enchants), `enchants clear`, `minimap` (toggle the minimap button), `reps0` (dev: dump uncategorized "Other" reps to a copy window), `debug on/off` (dev: toggle Summary column-boundary guides; bare `/wb debug` = status) |
| Warbandeer_Collected | `/collected`, `/collect` | `""` (open), `scan`, `refresh` (redraw the open window's grids from the current db, no rescan — the workaround for #718's intermittent blank grid), `wanted` (list sets flagged wanted), `outfit list\|save <name>\|load <name>\|delete <name>\|push <name>\|export\|post\|import <string or link>\|inspect` (the account-wide outfit library — save/load/delete a look that follows you across characters, `push` copies one into *this* character's per-character transmog sets, `export`/`post`/`import` share it as a `/customset v1` string or a clickable chat hyperlink (import takes either, including a shift-clicked link) — the same paths the dressing room's outfit row drives; `inspect` (#819) reads the TARGETED player's transmog into the same string via the inspect handshake, no room needed), `model <id>` (dev: preview a raw display id), `scale <n>` (dev: user scale multiplier; no arg dumps scale state), `normalize <0..1>` (dev: tune a race's normalization override), `release <1..12>` (dev: preview an expansion badge by release index), `coverage [all]` (dev: vet set resolution on live to a copy window; `all` lists every group, else empties only), `width` (dev: dump what is setting each open collection panel's width to a copy window — both grids' content, every chrome term, both modes' floors, and which binds; the two hosts render one shared `CollectedPanel` and are meant to come out identical, so this is how that gets checked rather than eyeballed), `profile [report\|show\|clear]` (dev: times the grid build and the Armor/Weapons swaps in **both** hosts — this window *and* `/wb` → Collected, which is why the profiler is published on the API global; **records by default, quietly** while #718's slow open is under investigation, so the bare call is the OFF switch; times Lua *and* the frames after it, because the two answers want opposite fixes; `report` aggregates the stored samples across sessions to a copy window, `show` toggles the per-run chat lines back on for active measuring, `clear` wipes them), `cleanup` (drop saved data for retired collection rows — never automatic, per the non-destructive DB rule; currently #653's Illusions/Arsenals groups) |
| Warbandeer_Decor | `/housingdecor`, `/wbdecor` | `""` (open), `scan` (re-scan the housing catalog), `wanted` (list decor flagged wanted) — `/decor` avoided (third-party decor-vendor addon owns it) |
| Recycle | `/recycle` | `clear`, `key CTRL|SHIFT|ALT` |
| Warbandeer_Bars | `/wbbars`, `/wbb` | `""` (status), `snapshot`, `list`, `restore <char> [specID]`, `forget <char> [specID]` |
| ShadowsOfUI-Ilvl | `/silvl` | `<itemID\|link>` (dev: dump an item's ilvl/quality/track) |
| ShadowsOfUI-Known | `/sknown` | `<itemID>` (dev: dump learnable-by list for a recipe item), `knownby <recipeID>` (dev: dump which crafters know a recipe), `guildcrafters <recipeID>` (dev: dump guild members who can craft a recipe) |
| ShadowsOfUI-Artisan | `/sartisan` | `[name]` (dev: dump a character's per-profession artisan-currency breakdown across alts) |
| ShadowsOfUI-Inventory | `/swinv` | `<itemID\|link>` (dev/find: dump an item's warband-wide count breakdown — per character, warband bank, guild banks) |
| ShadowsOfUI-Reputations | `/sreps` | `<factionID\|name>` (dev/lookup: dump every character's standing with a faction) |
| ShadowsOfUI-Quests | `/squests` | `<questID>` (dev/lookup: dump which characters are on / have completed a quest) |
| ShadowsOfUI-QuestXP | `/squestxp` | `""` (dev/lookup: dump the computed XP-reward percentage for the currently selected quest) |
| ShadowsOfUI-Delves | `/sdelves` | `""` (dev: dump live delve state for tier/name calibration), `dump` (print recorded run-time stats for the current delve) |
| ShadowsOfUI-Collectibles | `/scollect` | `""` (status), `custom` (open colour picker for the known-item tint), `itemtest` (dev: dump IsKnown/IsCollectible for the item under the cursor) |
| ShadowsOfUI-HousingVendor | `/shvendor` | `""` (status: indicator + surface toggles), `itemtest` (dev: dump owned/stored/bonus for the item under the cursor) |
| ShadowsOfUI-ProfCommissions | `/sprofcomm` | `""` (status: hook installed + icon size + money reserve + glow threshold), `size <n>` (dev: retune reward icon size live), `reserve <n>` (dev: retune the money-zone width the reward column aligns to), `glow <n>` (dev: retune the quality threshold a reward must reach for the gold rare glow), `rewards` (dev: dump every reward drawn this session with its resolved quality + whether it glows) |
| ShadowsOfUI-Compartment | `/scompartment` | `""` (open settings), `reset` (restore the compartment button's default position) |
| ShadowsOfUI-PostOffice | `/spost` | `""` (open settings) |
| ShadowsOfUI-Castbar | `/scast` | `""` (open settings), `dump` (dev: print text size + each bar's state/position) |
| ShadowsOfUI-Upgrade | `/supgrade` | `[name]` (dev: dump a character's available gear upgrades), `enchants [name]` (dev: copyable enchant-resolution dump — why a slot has no suggested enchant) |
