node_modules/
.env
data/
# ops/bot-ops.sh env-set backups. Once a deployment has migrated these live beside .env in the
# config dir, outside this checkout entirely — but an un-migrated host (or a dev running from
# source) still writes them here, and they hold the live token, so they must never be committable.
.env.bak.*
