# Warbandeer_Collected

**Deps:** LibNAddOn, LibNUI, Warbandeer_Characters · **SavedVars:** `WarbandeerCollectedDB` (v2) · **Commands:** `/collected`, `/collect` (no-arg opens the window; `scan` subcommand; `model <id>` / `scale <n>` dev verify) · **Reads:** `WarbandeerApi` · **UI:** LibNUI · **Compartment:** `WarbandeerCollected_OnAddonCompartmentClick` · **Exposes:** `WarbandeerCollectedApi` (read-only data bridge for Warbandeer's `collected` view)

Transmog set collection tracker. Renders a class × instance-set grid showing, per character, how many appearances of each tier/dungeon set remain uncollected, plus per-character instance lockouts.

## Files

| File | Purpose |
|---|---|
| `init.lua` | Assignment-form init (`local ns = LibNAddOn(...)`); `MigrateDB` seeds `db.sets`/`collected`/`total`, sets `version = 2` |
| `commands.lua` | empty command (`/collected` with no args) → `ns:Open()`; `scan` subcommand — rebuilds `db.sets`/`collected`/`total` from `C_TransmogSets` APIs, refreshes the open window; `model <id>` — dev: forwards to `ns.PreviewModelID(id)` to vet a raw creature display id; `scale <n>` — dev: forwards to `ns.PreviewModelScale(n)` to tune a race's `scale` |
| `data/sets.lua` | `ns.Sets` (set-group array) + `ns.Releases` (expansion names, Vanilla→Midnight) |
| `data/models.lua` | `ns.RaceModels` — per-race creature display IDs (an **enhancement** unlocking exact race+gender selection; hand-maintained, **verify in-game**). Two entry shapes: single-form `{ [2]=maleID, [3]=femaleID }`, or multi-form `{ forms = { { name, [2], [3] }, ... } }` (Worgen, Dracthyr) which makes the dressing room show a form toggle. ids must be **pre-baked** displays (`CreatureDisplayInfo` with `ExtendedDisplayInfoID != 0` — carries the race+gender's own textures); a bare `ChrModel` base display renders white, so `Dress` calls `DisplayInfo` with customizations off. 7 allied ids are recruitment-screen showcases; the rest were derived from DB2 (base `ChrModel.DisplayID` → `ModelID` → first baked `CreatureDisplayInfo` of that model). Also `ns.PlayableRaces()` (ordered `{id,name,file}` list for the selector, skipping ids absent on this client) |
| `api.lua` | Creates the global `WarbandeerCollectedApi` — read-only bridge so sibling addons (Warbandeer) can render the collection without depending on this addon's UI. Exposes `.Sets`/`.Releases` and `Counts()`, `IsScanned()`, `GroupStatus(groupId)`, `SetStatus(groupId, setId)` reading account-wide `ns.db`, plus `ShowInfoTip`/`HideInfoTip` and `ShowDressingRoom(group, set)`/`HideDressingRoom()` (lazy forwarders) so consumers render the identical tooltip + 3D preview window |
| `DataView.lua` | `ns.DataView` (TableFrame subclass) — lock + name + 13 class columns; cell value = uncollected count, tinted by a 10-shade red→green gradient; name click opens `LockoutView`, **class-cell click opens the 3D `DressingRoom`** (hover still shows `InfoTip`). `ToggleOrder()` flips row order oldest/newest-first (`_reverse`, **defaults newest-first**), tracking display vs `ns.Sets` source index so lockout selection stays correct |
| `controls/InfoTip.lua` | `ns.InfoTip` (CleanFrame) + `ns.ShowInfoTip(group, set, parent, pos)` / `ns.HideInfoTip()` — per-slot source list for a hovered set. Show/hide are **immediate** (no reposition debounce, no deferred hide); the dressing room opens from the grid cell's own `onClick`. Item names load async, so a render with blank rows requests the items (`C_Item.RequestLoadItemDataByID`) and re-renders (capped retries) until they resolve |
| `controls/LockoutView.lua` | `ns.LockoutView` (CleanFrame) + `ns.ShowLockoutView(grpIdx, parent, pos)` / `ns.HideLockoutView()` — character list, locked toons red and sorted first |
| `controls/DressingRoom.lua` | `ns.DressingRoom` (TitleFrame, `special`, `strata=HIGH`) + `ns.ShowDressingRoom(group, set)` / `ns.HideDressingRoom()` — persistent 3D preview: a `ui.Model` flanked by paper-doll equipment-slot columns (built/refreshed in the companion `DressingRoomSlots.lua`), a class-themed model backdrop (`_setBackground` — `dressingroom-background-<class>` atlas keyed to the set's class, behind the transparent ModelScene) with a **Background** toggle (`SetBackgroundOn`, shares the top control row with Undress, defaults on; `_setBackground`/`_applyBackground` re-assert the `dressingroom-background-<class>` atlas on a 50ms timer across a ~0.6s window (`_bgRetries`), not stopping at the first success — a one-shot set renders blank while the texture streams in (and `GetAtlasInfo` can briefly read nil), and each re-`Atlas` forces a render refresh; guarded so a class change mid-window can't reveal a stale backdrop), plus an Undress toggle (`SetUndressed` — bare body for race id). The body renders on a dressable player unit whose gender can't be overridden (see `Dress`), so gender isn't selectable (no toggle/indicator); `_sex` is tracked only for the per-sex `scale` (set in `_defaultToPlayer`). A wrapped race-icon selector whose icons use the **char's gender** (`raceicon-<file>-<male|female>` via `iconSex` from `UnitSex`, name-stub fallback) — `_defaultToPlayer` resets the selection to the logged-in char's race on each fresh open (while open, race picks stick; each icon has a `GameTooltip` naming the race), a **form toggle** (`SetForm`/`_setupForms`/`_highlightForm`) — a small floating button row just under the class icon (model upper-left), shown only for multi-form races (currently Worgen) whose `ns.RaceModels` entry has `forms`; a reusable 2-button pool, hidden for single-form races. A form may carry a `race` override (Worgen's "Human" form → `race = 1`, rendered as a plain Human); the other form omits it and renders the selected race — and a **scale slider** (`ui.Slider`) at the model's bottom-left with a readout: drives `Model:Scale` live (frame-level lifted above the model so the thumb is grabbable), reset to the race's `scale` by `Dress`; dev `/collected scale <n>` drives it too via `ns.PreviewModelScale`. A directional nav pad floats in the model's upper-right corner (a 4-button `<`/`>`/`^`/`v` cross, parented to `self` and lifted to `model:Level()+10` so the mouse-enabled ModelScene doesn't eat the clicks): `<`/`>` call `Step(dir)`, `^`/`v` call `StepTier(dir)`. The **class icon** mirrors the pad in the model's upper-left corner (`classBox` frame at `navLvl`, sized to the cross's `NAVSPAN` footprint so the two top corners are symmetric) with a `GameTooltip` naming the class on hover (`_className`, set by `_showClass`); the title bar carries only the title text + close button. `Step(dir)` cycles the current group's class sets (wraps, skipping the empty `{}` class slots) — also driven by the **Left/Right arrow keys** via an `OnKeyDown` handler (keyboard-enabled frame; all other keys propagate so default bindings + Escape still work). `StepTier(dir)` (**Up/Down arrow keys** / the pad's `^`/`v`) keeps the class column but switches to a sibling **difficulty tier** of the same raid — the `ns.Sets` groups sharing this group's base `id` (e.g. Hellfire Citadel Normal/Heroic/Mythic = id 28), in difficulty order, wrapping; falls back to the tier's first real set if it lacks the current class column. `_load(group, set)` is the shared "preview this set" path (title + class icon + `UpdateSlots` + `Dress`) used by `Step`, `StepTier`, and `ns.ShowDressingRoom`. `Dress()` always renders the race on a **dressable** actor via `Model:Unit("player", raceID)` (where `raceID` is `form.race` if the selected form overrides it, else the selected race; textured, gender follows the char) so the set can actually be worn and Undress works — the exact-gender `Model:DisplayInfo` creature-display path is a static NPC that can't wear transmog (its `ns.RaceModels` `[2]/[3]` ids are no longer used for the body; only the entry's `scale` is). The outfit is set via `Model:Outfit` **before** the (re)load — the sourceID list from `C_TransmogSets.GetAllSourceIDs(set.id)`, or empty when Undress is on — so the model re-applies it on its async load callback (a one-shot `TryOn`/`Undress` would be wiped when the model finishes loading in its default body). `ns.PreviewModelID(id)` forces a raw display id into the open model (dev `/collected model`) |
| `controls/DressingRoomSlots.lua` | Reopens `DressingRoom` to add `:_buildSlots(winW)` (the two paper-doll columns — Head/Shoulder/Back/Chest/Wrist left, Hands/Waist/Legs/Feet right, each centered independently) and `:UpdateSlots()` (fills each slot with the set piece's icon + green/red status border + an in-game `GameTooltip` on hover, capped icon-load retry; slots the set doesn't use show the shared `ui.media.unresolved` marker). Owns the slot-status colors + slot/column layout constants; borrows `_selBox`/`_IDLE`/`_PAD`/`_MODELH` from `DressingRoom` and exports `DressingRoom.MODEL_INSET` (model edge inset) back to the constructor |
| `window.lua` | `MainWindow` (TitleFrame, `special`, `level=580`): DataView + ScrollFrame + `Sets: x / y` counter + a raid-order toggle button in the title bar (left of close; calls `DataView:ToggleOrder`, border goes gold when newest-first); `ns:Open()`, `ns:CompartmentClick(btn)` |

## Data Model (`ns.Sets`)

```lua
{ id, name, release, instance, difficulty, minLevel?,
  sets = { { id, name, classId }, ... } }  -- one entry per class set; id is the base set id
```
`release` indexes `ns.Releases`; `instance`/`difficulty` key into a character's `instances.locks` (from the data layer) for lockout display.

## SavedVariables (`WarbandeerCollectedDB`)

```lua
{ version = 2, collected = int, total = int,
  sets = { [groupId] = { [setId] = true | { collected, parts, total } } } }
```
- `true` = base set fully collected; otherwise a partial record (`parts` is the raw `GetSetPrimaryAppearances` array).
- `MigrateDB` is non-destructive: only ensures missing top-level keys exist.

## Scan Logic (`/collected scan`)

Resets the three DB keys, then for each group → each set with an `id`:
- `C_TransmogSets.IsBaseSetCollected(id)` true → store `true`, bump `collected`.
- else → `GetSetPrimaryAppearances(id)`, count `p.collected`, store `{ collected, parts, total }`.

`total` accumulates `#grp.sets`. After scanning, refreshes `ns.window.counter` and `ns.window.data`.

## Gotchas

- **Grid cells show *uncollected* count, not collected** — `text = total - collected`, and the gradient shade is keyed by the *collected* fraction (`floor(collected/total*10)`), so a low number on a green cell means nearly done.
- **InfoTip slot order is fixed** — it walks slots `{1,3,5,6,7,8,9,10}` (Head, Shoulder, Chest, Waist, Legs, Feet, Wrist, Hands), matching `GetSourcesForSlot` against the set's primary `appearanceID`s; collected sources green, missing red.
- **Lockout state lives in the data layer**, not here — read via `api.GetAllCharacters()` → `toon.instances.locks[group.instance][group.difficulty]`.
- **`ns:CompartmentClick`**: right-click runs the scan, any other click opens the window.
- **DataView auto-sizes the name column** at construction from the widest row label, then grows `rowArea`/self to match.
- **Arbitrary races need a `ModelScene` actor, not a bare `DressUpModel`.** `SetCustomRace` was removed, and `DressUpModel:SetDisplayInfo` renders non-player races untextured (white). The `ui.Model` widget borrows dressup scene 596 and skins its actor via `SetModelByCreatureDisplayID` (exact race+gender, needs a display ID) or `SetModelByUnit("player", …, customRaceID)` (any race, textured, gender = the char's). `ns.RaceModels` only supplies the former; it is hand-maintained and unverified beyond the Allied Races ids — verify in-game.
- **InfoTip show/hide is immediate.** Cell `onEnter` re-anchors and renders right away; `onLeave` hides at once. (An earlier version debounced reposition and deferred the hide so a **Preview** button inside the tip stayed clickable — both removed; the dressing room now opens only from the grid cell's `onClick`.)
</content>
</invoke>
