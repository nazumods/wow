# Warbandeer (Main UI)

**Deps:** LibNAddOn, LibNUI, Warbandeer_Characters · **OptionalDeps:** Warbandeer_Bars (`bars` view), Warbandeer_Collected (`collected` view), ShadowsOfUI-Upgrade (gear-upgrade markers) · **SavedVars:** `WarbandeerDB` (v3) · **Commands:** `/warband`, `/wb` (+ one per view) · **Reads:** `WarbandeerApi`, `WarbandeerBarsApi`, `WarbandeerCollectedApi`, `ShadowsOfUI_UpgradeApi` · **UI:** LibNUI

Main viewer UI. Reads the data layer (`ns.api` ← `WarbandeerApi`) and renders it across a set of views switched from a left icon rail.

## Files

| File | Purpose |
|---|---|
| `init.lua` | Addon init (assignment form) + `ns:RegisterSettings` (`defaultView`, `tooltipSide`; the per-column **Summary Columns** subcategory is registered separately in `views/summaryColumnsSettings.lua`). Defines `ns.views`, `ns.viewOrder` (nav order), class/race arrays, `MigrateDB` (v4 seeds `settings.summaryColumns`), `onLoad`. **Two live refreshers** wired in `onLoad`, each generation-guarded + debounced and gated on the relevant view being the one shown: `PLAYER_EQUIPMENT_CHANGED` → re-runs Detail's `OnBeforeShow` for the logged-in character (`DETAIL_REFRESH_DELAY` 750ms; see DetailView); `PLAYER_ACCOUNT_BANK_TAB_SLOTS_CHANGED`/`BANKFRAME_CLOSED` → re-runs the shown Summary/Gear `OnBeforeShow` (`BANK_REFRESH_DELAY` 2100ms) so a cross-character upgrade marker clears the moment its item is withdrawn from the **warband bank** — the data layer rebuilds `db.bank.warband.equip` on the same events (a remaining duplicate stays listed), and the long delay outlasts both that rescan and ShadowsOfUI-Upgrade's ~2s memo TTL so `OnBeforeShow` re-reads the fresh list, not a cached upgrade result |
| `data.lua` | `ns.data` — ilvl/gear-tier color helpers (`IlvlColor`, `IlvlColorObj`), faction color/standing override tables, profession-intent helpers (`GetProfIntent`/`SetProfIntent`, `GetMainCrafter`, `GetProfToons`, `FindProf`, `EstimateConcentration`). Also `ns.gearSlots` (draw order), `ns.factionIcon`, and `ns.gearSlotIcon` (per-slot `{path,coords}` into the `icons/gearslots.tga` atlas — a 4×4 grid of 256px cells; Finger1/2 alias the ring cell, Trinket1/2 the trinket cell) |
| `theme.lua` | `ns.theme` — a LibNUI `ui.Theme` ("void-dark"): `colors` + `fonts` (`{path,size}` tuples). Passed on MainWindow, so every widget in the window inherits it; `window`/`border`/`divider`/`text`/`muted`/`header` and fonts `title`/`body` override the LibNUI dark defaults, the rest (`module`, `hover`, `gold`, …) are Warbandeer-specific tokens |
| `media/fonts/` | Bundled fonts (Hanken Grotesk, Geist, JetBrains Mono) + licenses; loaded by path, not listed in `.toc` |
| `controls/CharacterTooltip.lua` | `ns.CharacterTooltip` + `ns.ShowCharacterTooltip`/`HideCharacterTooltip`; side-aware anchoring via `ns.TooltipSide()` / `ns.AnchorTip(frame)`. Registered on `ns`, not `ui` |
| `controls/ItemTooltip.lua` | `ns.ShowItemTooltip(frame, link, compareLink?, noUpgradeBlock?)` / `ns.HideItemTooltip()` — shared private `WarbandeerItemTooltip` (GameTooltipTemplate) reskinned (`styleTooltip` → flat black + theme border via `NineSlice`) that `:SetHyperlink`s an item link; WoW auto-comparison off. `compareLink` puts the suggested upgrade on the *anchored* tip (closest to the row/cursor, like the item under consideration in a normal comparison) and shows the equipped item on a second private tooltip (`WarbandeerItemCompareTooltip`) extending outward beside it (away from the row per `TooltipSide`), carrying an integrated "Equipped" tab. Tooltips use a dark near-black `BORDER` (the theme tan reads too bright here). Detail uses this to lay the upgrade next to the equipped item. `noUpgradeBlock` sets `tooltip.SkipUpgradeBlock` so ShadowsOfUI-Upgrade's "Upgrade for:" line is suppressed (compare tooltip always opts out). Used by Detail + Gear gear cells; anchor side per `ns.TooltipSide()`. `ns.ShowEnchantTooltip(frame, suggestion)` reuses the same `itemTip` frame for a focused **recommended-enchant** tooltip (Detail's "Missing enchant" note hover): a gold name header + a green wrapped **effect description** of what the enchant grants (via `enchantText` → `GetSpellDescription` for a spell id, or `GetItemSpell` → its use-spell description for an item id) — deliberately *not* the scroll/recipe's full item tooltip (reagents/sell price). `ns.ShowGemTooltip(frame, suggestion)` is the gem sibling (Detail's "Empty socket" note hover): a gem has no use-spell, so it just `:SetItemByID`s the gem (its own short item tooltip carries the stat bonus = what it does). Both hidden by `ns.HideItemTooltip` |
| `controls/StatCard.lua` | `ns.StatCard` — summary tile (caption + big mono `amount`, optional `sub` + trend `subIcon`). `Amount(text, color?)` |
| `controls/StatChart.lua` | `ns.StatChart` — compact secondary-stat radar (outline only, no fill). Four vertices along the diagonals — Crit (TL), Haste (BL), Versatility (BR), Mastery (TR) — joined into a quadrilateral over a faint square guide. Each vertex's distance is the stat's **fulfilment vs its Archon target** — 1.0 lands on the guide square, capped at `overshoot`× (default 1.2, which reaches the frame corner) so an over-target stat pokes a little past the guide — so under-target stats pull toward the centre and met/over ones reach or pass the guide (matching the red/blue target cells beside it); when a stat has no target it falls back to its rating relative to the largest of the four (balance plot). Edges are WoW native frame lines (`CreateLine`), vertex dots are nine-sliced `bar-rounded` textures; `Set(crit, haste, mastery, vers, hot?, targets?)` re-plots and tints any `hot` (tier-1) stat's dot gold (all-zero hides the polygon). Re-derived from the idea behind Narcissus's radar chart, not its mask-texture implementation |
| `controls/IconStrip.lua` | `ns.IconStrip` — floating left nav rail, one tinted glyph per view; `onSelect(name)`, `SetActive(name)` |
| `controls/UpgradeMark.lua` | `ns.UpgradeMark(charName, slot)` → cell-text ▲ glyph (green held / gold better-in-warband), `ns.UpgradeTip(charName, slot)` → hover line, `ns.UpgradeSuggestion(charName, slot)` → `link, ilvlGain, warband` for the upgrade item (drives Detail's per-slot sub-line), `ns.MissingEnchantSlots(charName)` → `{ [slot]=true }` set of equipped slots missing a permanent enchant (built once per Detail render, drives the gear-row "Missing enchant" note), `ns.RecommendedEnchant(charName, slot)` → the recommended enchant's **name** (resolves the API's `EnchantSuggestion` via the shared `suggestionName`: its own `name` from ClassCodex, else the id via `C_Spell.GetSpellName`/`C_Item.GetItemInfo`; Detail note + Summary "Ench" hover), `ns.RecommendedEnchantInfo(charName, slot)` → the **raw** `EnchantSuggestion` (kind/id/name, not just the name) so a consumer can render the enchant's own item/spell tooltip (Detail's "Missing enchant" hover → `ns.ShowEnchantTooltip`), `ns.EmptySocketSlots(charName)` → `{ [slot]=count }` of equipped slots with empty gem sockets (Detail note + Summary "Gem" hover), and `ns.RecommendedGems(charName)` → `primary, secondary` recommended gem **names** (the unique diamond + the repeatable fill gem; same resolver, either may be nil), and `ns.RecommendedGemsInfo(charName)` → the **raw** `primary, secondary` gem suggestions (kind/id, for the Detail "Empty socket" hover → `ns.ShowGemTooltip`); all via `ShadowsOfUI_UpgradeApi` (OptionalDep); no-op "" / nil / empty-set when that addon is absent. Also the **wrong-enchant** trio: `ns.EnchantMismatchSlots(charName)` → `{ [slot] = {applied, recommended, itemID, enchantID} }` (the RAW mismatch set, *unfiltered* by the accept list, so the view can still offer an un-accept on an accepted row), plus the per-item accept toggle `ns.IsEnchantIgnored(itemID, enchantID)` / `ns.ToggleEnchantIgnore(itemID, enchantID)` backed by `WarbandeerDB.ignoredEnchants` (key `"<itemID>:<enchantID>"`, account-wide) |
| `controls/SuggestedBox.lua` | `ns.SuggestedBox` — Detail's left-column "SUGGESTED" panel: the highest-impact gear actions for the character, merged across every source and ordered by ilvl gain. `Populate(char)` (caller sets width first) gathers candidates from `ShadowsOfUI_UpgradeApi:CharacterUpgrades` (ready gear, held green / warband gold), `:WorldQuestUpgrades` (orange) and `:VendorUpgrades` (faction-quartermaster, cyan), drops items whose required level exceeds the character's, then **keeps the single biggest upgrade per slot** (a better vendor piece replaces a smaller bank/WQ one) and **sorts by ilvl gain desc** — so the most impactful action leads regardless of source. Ties break toward the cheapest action (held/warband > WQ > vendor). Fills ≤5 pooled **two-line** rows: a headline `{icon} [Item] +N ilvl` (gain tinted by source) over a smaller muted `bodySmall` sub-line spelling out the source — the slot (`Main Hand`) for ready gear, `WQ: <title> — <zone>`, or `Buy: <quartermaster> (<zone>) — <cost>` — so the detail gets the row's full width instead of truncating. Row hover lays the upgrade beside the equipped piece via `ns.ShowItemTooltip`. **Clicking a WQ row opens the world map to the quest's zone (`mapID`) and super-tracks it; a vendor row opens the map to the quartermaster's zone** (no quest to super-track); a **ready row (held/warband) flashes the item across the open bags/bank** (`ns.FlashItem` on `_compareLink`/`_itemLink`). The WQ + vendor sources degrade silently on an older Upgrade addon without the respective method. Shows "Nothing pressing — looking good!" when there are none; hides + returns 0 height when ShadowsOfUI-Upgrade (OptionalDep) is absent |
| `controls/FlashItem.lua` | `ns.FlashItem(link)` — "flash find": pulse every on-screen bag/bank slot holding the item (matched by itemID), so clicking a suggested upgrade locates it. **Detect-and-adapt**: fires Bagnon's own `FLASH_ITEM` signal (`LibStub("WildAddon-1.1", true).Calls:Fire("Bagnon.FLASH_ITEM", id)`, guarded — Bagnon replaces the default bag UI so its frames must pulse natively; the WildAddon signal is tagged by the **front-end** addon owning the live buttons — `Bagnon.` — not the shared `BagBrother` engine) **and** walks Blizzard's default container frames (`ContainerFrameUtil_EnumerateContainerFrames` → `flashBlizzard`) + the `BankPanel` item-button pool, playing a private additive border-glow overlay on each match (Blizzard's built-in `flashAnim` can't be reused — the new-item OnUpdate stops it next frame for a non-new item). Opens the bags first (`OpenAllBags`); a bank-only item flashes only while a banker is open. Wired to Detail gear-row clicks (`ns.detail.attachItemTip` → `_upgradeLink`) and Suggested-box ready-row clicks (`_compareLink`/`_itemLink`) |
| `controls/FilterDropdown.lua` | `ns.FilterDropdown` — reusable labelled dropdown filter. `options`/`selected`/`onSelect`; `Select(key)` re-points label without firing |
| `controls/LabeledBar.lua` | `ns.LabeledBar` — progress row (name + value + bar beneath). Setters `Fill`/`Label`/`Value`/`BarColor`; optional `onClick`, `hoverValue`/`hoverColor` |
| `media/bar-rounded.tga` | 16×16 white rounded-rect, nine-sliced as the `LabeledBar` bar texture; tinted at runtime |
| `icons/views/*.tga` | White per-view glyphs (+ `logo.tga`) tinted in-game by `IconStrip` |
| `icons/trending_{up,down}.tga` | White up/down trend arrows for the Overview weekly-gold card |
| `icons/*.tga` | White 64×64 glyphs for Summary column headers (`crest_hero`, `crest_myth`, `catalyst`, …), tinted muted in-game |
| `views/Overview.lua` | Stat strip + Reputations / Achievements / Top Characters modules; titlebar `BuildFilter` carries a raid picker + an expansion picker. Rep bars via `FactionBars` (see below); rows click through to Detail. The `TopAlts` table appends one column per raid difficulty (RF/N/H/M) showing the selected raid's per-class transmog-set completion — uncollected piece count (red→green shaded) or a green check — read from `WarbandeerCollectedApi` (OptionalDep) via `SetStatus`; `RAIDS`/`DIFFS` drive the raid dropdown + columns, `TopAlts:SetRaid`/`Refresh` re-render, and `Overview:OnBeforeShow` refreshes on each open |
| `views/SummaryColumns.lua` | `SummaryColumn` specs (`getData`/`getFooter` per column; each toggleable column carries a stable `key` + settings `label`, identity columns have neither) + `ns.VisibleSummaryColumns()` (the column list filtered by `db.settings.summaryColumns[key]`, identity columns always kept) + `SummaryColumnsDelayed()` (appends the dynamic DMF column to a table's `_columns`/`addCol` while the faire is open — **not** inserted into the global `ns.SummaryColumns`, so it's always-shown-while-active and never appears in the settings panel) |
| `views/summaryColumnsSettings.lua` | Builds the **Summary Columns** settings subcategory (nested under "Warbandeer"): one checkbox per toggleable column (those with a `key`), default on, writing `db.settings.summaryColumns[key]` and calling `ns.RefreshSummaryColumns()` on change. Loads after every `summaryCol/*.lua` so `ns.SummaryColumns` is complete (gated Up/Ench/Gem checkboxes appear only when ShadowsOfUI-Upgrade is loaded) |
| `views/summaryCol/*.lua` | One file per Summary column: faction, role, character, level, ilvl, upgrades, enchants, gems, catchup, profs, bags, mail, vault, keystone, crests, catalyst, voidcore, manacrystal, delves, lumber, cofferKey, caches, xp, rested, played, gold. `mail.lua` (the "Mail" column — plain text header, no icon) shows each character's mailbox item/gold count via `t.mail`, **red** when the soonest piece expires within 3 days; blank when the mailbox is empty or unscanned. Each toggleable column passes a `key` + `label`; the five identity columns (faction, role, character, level, ilvl) pass neither and are always shown. `manacrystal.lua` (Untainted Mana-Crystals, currency 3356) shows the held count, red when the 250 weekly-earn cap is reached, hover = earned/cap; muted `crystal` glyph header (`icons/manacrystal.tga`). `catchup.lua` (the "<180" column) counts Head/Neck/Hands/Waist slots below ilvl 180 for level-81+ characters (muted em-dash below 81; green check at 0). `xp.lua` shows current XP as a percent of the level's total (hidden at max level). `upgrades.lua` (the "Up" count column — gold by default, **green** when at least one listed upgrade is at-or-below the character's level, i.e. equippable right now regardless of location, a warband-bank copy can be withdrawn; stays gold only when every upgrade is still gated above their level) and `enchants.lua` (the "Ench" column — count of equipped slots missing a permanent enchant, green check at 0, muted em-dash below max level; hover lists slot + item + the recommended enchant via `ns.RecommendedEnchant`, click → Detail), and `gems.lua` (the "Gem" column — total empty gem sockets across equipped gear via `MissingGems`, green check at 0, em-dash below max; hover lists slots + the recommended gems via `ns.RecommendedGems` — the unique diamond "×1" then the fill gem) all early-return unless `ShadowsOfUI_UpgradeApi` exists, so those columns are only registered when ShadowsOfUI-Upgrade is loaded |
| `views/SummaryView.lua` | Dual `ClassSummary` tables (Alliance/Horde) toggled by a faction `BuildFilter`; cells drive row hover + click-to-Detail. Each table is built from `ns.VisibleSummaryColumns()` (resolved fresh per build, so a column toggle is reflected on rebuild); `buildColInfo(cols)` derives the header colInfo and each table stores its own `_columns` list (row builders walk it; the dynamic DMF column is appended to it) |
| `views/GearView.lua` | Four armor-type tables toggled by `BuildFilter` buttons; per-equipment-slot ilvl + upgrade-track columns. Each slot cell hovers to the shared item tooltip (`ns.ShowItemTooltip`) and appends `ns.UpgradeMark(toon.name, slot)` (the ▲ available-upgrade glyph) |
| `views/DetailView.lua` | Single-character detail (class def + constructor + `Select`/`OnNavigate`/`OnBeforeShow`; the split-out helpers below re-open `ns.views.DetailView`). Header: a faction icon stacked over a role icon, then a gender-aware race icon (`ns.detail.raceAtlas` → `raceicon128-<race>-<sex>` atlas, with a `RACE_ATLAS_FIX` table for race-file tokens that differ from the atlas spelling), then the class portrait, then the identity text (name over a `race + active-spec + class` subtitle — e.g. "Human Shadow Priest"; the spec drops out for an alt scanned before it was captured). Below: stat strip (Item Level + Playtime cards), a **secondary-stat 2×2 grid** (`detail/Stats.lua` → `_showStats`, slotted at `D.STATS_TOP` between the cards and the professions block — `D.PROF_HEADER_Y` is offset by `D.STATS_H` to make room), per-profession intent panels (each followed by that profession's equipped tool/accessory list), equipped-gear list. Gear + prof-gear rows highlight and show the shared item tooltip on hover (`ns.detail.attachItemTip` → row `hover` tint + `ns.ShowItemTooltip`, always with `noUpgradeBlock` so the "Upgrade for:" line is suppressed; see `controls/ItemTooltip.lua`); when an upgrade exists, equipped-gear rows show the suggested upgrade (`ns.UpgradeSuggestion`) on a smaller sub-line beneath the item name, rendered as the item link's own appearance (`[Name]` in rarity colour) + the ilvl gain (tinted green held / gold warband) + `@ lvl N` in red when the item needs a level this character hasn't reached (`ns.detail.reqLevel` via `GetItemInfo`) — no ▲ arrows. Below the upgrade, a slot **missing its permanent enchant** (`ns.MissingEnchantSlots`, precomputed per render into `self._missingEnch`), a **wrong (non-recommended) enchant** (`ns.EnchantMismatchSlots` → `self._enchMismatch`, shown only when not on the accept list — yellow "Wrong enchant (have X) → recommend Y", `shortEnchant` trims the "Enchant <Slot> - " prefix; **right-click the row to accept/un-accept** that enchant on the item via `ns.ToggleEnchantIgnore`, handled in `detail/Layout.lua`'s shared row `OnMouseUp`, which then re-runs `OnBeforeShow`+`Fit`; **accepting shows a confirm popup** (`StaticPopupDialogs.WARBANDEER_ACCEPT_ENCHANT`) since it silences the flag, while un-accepting is immediate; missing and wrong are mutually exclusive), and/or an **empty gem socket** (`ns.EmptySocketSlots`/`self._emptySockets`) each add a note — orange "Missing enchant" / yellow "Wrong enchant" / cyan "Empty socket" — with the recommendation appended ("→ Enchant Ring – …" via `ns.RecommendedEnchant`; "→ `<gem>`" via `ns.RecommendedGems`). The gem is split: the **unique diamond** (`self._gemPrimary`) goes on the *first* empty socket only (tracked by `self._gemPlaced` across the gear loop), every other socket gets the repeatable fill gem (`self._gemSecondary`). **Each present line (upgrade, enchant, socket) renders on its own pooled sub-line label** (`row.subs`, up to `GEAR_MAX_SUBS`=3, each anchored `GEAR_UP_H` below the previous; the row grows one `GEAR_UP_H` per shown line — a single multi-line label can't be used, `wordWrap=false` collapses `\n` to one truncated line). Each line is inline-coloured so the label's muted base doesn't override it (upgrade green/gold via `GREEN_CODE`/`GOLD_CODE`, notes orange). The name column autosizes to fit the wider of name / widest sub-line, and the upgrade's link is stashed on the row frame (`_upgradeLink`) so hovering shows it as a side-by-side comparison tooltip next to the equipped item, and **left-clicking the row flashes that upgrade item in the open bags/bank** (`ns.FlashItem`). Beneath the professions block in the left column sits the `ns.SuggestedBox` "SUGGESTED" panel (anchored where the next profession panel would go, width `PANEL_W`, height folded into `leftH` in `OnBeforeShow`) listing the character's immediately-equippable gear upgrades. Character-picker `BuildFilter`; `Select(toon)` switches subject; `OnNavigate()` resets to the logged-in character. **Live gear refresh:** `init.lua` registers `PLAYER_EQUIPMENT_CHANGED` and, when the Detail view is the shown view for the logged-in character, re-runs `OnBeforeShow` (+`MainWindow:Fit`) after a `DETAIL_REFRESH_DELAY` (750ms, outlasting the equipment broker's async rescan) debounce — so a suggestion clears the moment the gear changes. This matters most for the Suggested box's fixed vendor entries, which (unlike held/WQ ones) never drop out of an inventory cache and so are gated only by the equipped-ilvl comparison. `MainWindow:ShownView()` (window.lua) returns the visible view or nil so the refresher only fires when Detail is actually on screen |
| `views/detail/Layout.lua` | `ns.detail` — shared Detail layout metrics (`P`/`GAP`/`PANEL_W`/`GEAR_*`/`PG_*`/header-icon sizes …) + leaf helpers (`attachItemTip`, `rarityColor`, `reqLevel`, `tierAtlas`, `intentColor`, `raceAtlas`, `INTENT_OPTIONS`, `CHEVRON`, `gearInnerW`/`gearPanelW`). Loaded before `DetailView.lua`; the other detail files alias `local D = ns.detail` |
| `views/detail/Professions.lua` | DetailView profession-row pool: `_profRow` (icon + skill `LabeledBar` + intent `FilterDropdown`), `_profGearRow` (equipped tool/accessory sub-rows), `_showProf` |
| `views/detail/Gear.lua` | DetailView equipped-gear row pool: `_gearRow` (slot icon + name + ilvl + track + stacked sub-lines), `_showGear`. Each row also carries two pooled transparent mouse-catchers (`row.enchHover` / `row.gemHover`, built by `noteHover`, parked by `parkHover`) repositioned over the **missing-enchant** / **empty-socket** sub-lines each render (a slot may have both); hovering one shows that recommendation's own tooltip in place of the row's equipped-item tooltip — `ns.ShowEnchantTooltip` (enchant effect, raw `ns.RecommendedEnchantInfo`) / `ns.ShowGemTooltip` (gem stat, raw `ns.RecommendedGemsInfo`) |
| `views/detail/Stats.lua` | DetailView secondary-stat 2×2 grid (Crit + Mastery / Haste + Versatility) under the stat cards: `_buildStatGrid` (lazy, 4 module-bg cells — two narrowed columns with `ns.StatChart` filling the centre channel between them, sized `D.STATS_CHART`) + `_showStats` (fills `pct`/`rating` from the data layer's `stats.secondary`, blanks `—` when unscanned, and feeds the four gear ratings to the centre radar via `StatChart:Set`). The spec's tier-1 stat(s) are tinted gold via `ShadowsOfUI_UpgradeApi:StatRanks` (OptionalDep). When `:StatTargets` returns an Archon target the rating shows as "current / target" with the current `deltaState`-coloured (±5% green = on-target, blue = over, red = under); each cell is hover-enabled (`showStatTip`) and shows a colour-matched status tooltip ("In/Over/Under target" + signed delta + current/target) when it has a target. A child hit-frame over just the percentage value (`showPctTip`, tracking the label as its text resizes) shows a separate plain-language tooltip explaining what that percent means in game (`EXPLAIN` per stat) — it captures the hover in place of the cell's target tooltip over the number, leaving the rest of the cell on the target tip. The **mastery** cell overrides the generic note with its **spec-specific** mastery (`masteryOverride` → "Mastery: \<name\>" + the passive's own description, resolved live from the captured `stats.secondary.mastery.spell` id), falling back to the generic note for characters not yet re-scanned with the spell id |
| `views/detail/Filter.lua` | DetailView `BuildFilter` (titlebar character picker) + `sortedCharacters` |
| `views/RoleView.lua` | `ClassTable` per class, grouped by spec |
| `views/RaceView.lua` | 13×29 class/race grid (dynamic build), one character per cell; hover + click-to-Detail |
| `views/Legion.lua` | Hidden artifact appearances + Legion achievements |
| `views/Midnight.lua` | Midnight achievement grid |
| `views/ProfsView.lua` | Best-skill-per-expansion grid + per-character detail panel |
| `views/MidnightProfs.lua` | Profs × characters grid: Midnight skill + concentration |
| `views/CraftingView.lua` | Crafting profs: main crafter, concentration, learned-recipe %; expansion `BuildFilter` |
| `views/PlaytimeView.lua` | Per-character playtime breakdown |
| `views/BarsView.lua` | Action-bar profile browser (class/spec filters + result list); needs `WarbandeerBarsApi` (OptionalDep) |
| `views/BarsPreview.lua` | `ns.BarsPreviewFrame` — companion box docked right of the window rendering a profile's bars (icons, keybinds, Edit Mode layout) |
| `views/BarsApply.lua` | `ns.BarsApplyFrame` — companion box below the main window: per-bar muted/red toggles (1-8 / C1-C5 / Bonus, Sky, Pet) + Apply button → `WarbandeerBarsApi:Restore` with a `barFilter` |
| `views/CollectedView.lua` | Transmog-set collection grid (class × set-group, red→green uncollected counts + green checks) backed by `WarbandeerCollectedApi` (OptionalDep); local `Grid` TableFrame inside a capped `ScrollFrame`, cell hover → the shared per-slot source InfoTip via `WarbandeerCollectedApi:ShowInfoTip`/`HideInfoTip` (same tooltip as `/collected`, including its **Preview model** button → the shared 3D dressing room — inherited for free since the InfoTip is one shared singleton), anchored on the configured side via `tipPosition` (`ns.TooltipSide()`); `BuildFilter` titlebar toggle flips row order (oldest/newest first, in-place re-sort via `grid._reverse`). No lockout columns/panel — those stay in Collected's own window |
| `window.lua` | `MainWindow` (TitleFrame) + `IconStrip` rail; `ns:Open()`, `ns:view(name)`; `Fit()` grows the window down/right. `ns.RefreshSummaryColumns()` forgets+rebuilds the Summary view (re-showing it if visible) when a column show/hide setting changes |
| `commands.lua` | Registers the base open command + one per view (from `ns.views`), plus `enchants` (list accepted wrong-enchants from `WarbandeerDB.ignoredEnchants`, resolving item names) and `enchants clear` (wipe the accept list + re-render the open Detail view) |

## Views

| name | _title | Parent | Key feature | `BuildFilter` |
|---|---|---|---|---|
| `overview` | Overview | Frame | Stat strip, reputations, achievements, top characters (per-raid RF/N/H/M set completion) | raid + expansion dropdowns |
| `summary` | Summary | Frame | Dual ClassSummary tables (Alliance/Horde) | faction toggle |
| `detail` | Detail | Frame | Per-character detail + profession-intent editor + per-profession gear list | character picker |
| `gear` | Gear | Frame | 4 armor-type tables, per-slot columns | armor-type buttons |
| `roles` | Roles | Frame | ClassTable per class, grouped by spec | — |
| `races` | Races | TableFrame | 13×29 grid, one character per cell | — |
| `profs` | Professions | Frame | Profession skill grid + detail panel | — |
| `crafting` | Crafting | Frame | Main crafter, concentration, recipe % | expansion dropdown |
| `midnight` | Midnight | Frame | Achievement grid | — |
| `legion` | Legion | Frame | Hidden artifacts + achievements | — |
| `playtime` | Playtime | Frame | Per-character playtime | — |
| `midnightprofs` | Midnight Profs | Frame | Profs × characters: skill + concentration | — |
| `bars` | Bars | Frame | Bar-profile browser + docked preview & apply panels | — (view-local dropdowns) |
| `collected` | Collected | Frame | Transmog-set collection grid (class × set-group) backed by `WarbandeerCollectedApi`; cell hover shows the source `InfoTip`, cell click opens the 3D `DressingRoom` | raid-order toggle (defaults newest-first) |

`BuildFilter(parent)` widgets show in the title bar only while that view is active. The two
expansion dropdowns + the Overview raid picker share `ns.FilterDropdown` (Overview returns a
container holding both its raid and expansion pickers); the faction toggle, character picker,
and armor-type strip are view-local.

## MainWindow

Subclasses `TitleFrame`; `special=true`, `level=600`, `theme = ns.theme` (inherited by all window widgets; table headers default to the theme's muted `header` token, so views no longer set header colors explicitly).
- **Views are constructed lazily**: `MainWindow:getView(name)` builds a view (and wires its `BuildFilter`) on first navigation; only the default view is built when the window opens. The nav rail therefore reads **class-level** metadata — every view class sets `name`, `_title` (and optionally `_iconRotation`) as class fields after `Class(...)`, not (only) in defaults. Row-click cross-navigation must go through `w:getView("detail")`, never `w.views.detail` (may not exist yet).
- `self.iconStrip` — `IconStrip` rail docked just left of the window, one glyph per view in `ns.viewOrder` order (unlisted views appended, sorted by title). Clicking a glyph calls the view's `OnNavigate()` (if defined) then `self:view(name)`.
- `MainWindow:view(name)` — hides current, shows named (building it if needed), updates title+size, calls `iconStrip:SetActive(name)`.
- Window is anchored by a single **TOPLEFT** point (stored in `db.settings.windowPos`) so view changes grow it down/right instead of re-centering. `SavePosition`/`RestorePosition` persist/apply it; titlebar drag saves on release.
- `ns:Open()` lazy-creates the window; `ns:view(name)` = `Open()` then `OnNavigate()` (if the view defines it) then `view(name)`. Both the icon rail and slash commands count as *direct navigation* and fire `OnNavigate`; row-click paths (`w:getView("detail"):Select(toon)` + `w:view("detail")`) call `MainWindow:view` directly and skip it, so the clicked character stays selected.

## Overview — Factions Widget

`FactionBars` (Frame subclass) stacks one `LabeledBar` per major faction plus optional subfaction
rows; data built by file-local `gatherFactions`. Bar **fill = faction colour**, resolved by
`colorFor(id, apiColor, fallback)`: `ns.data.factionColors[id]` override → API `factionFontColor` →
fallback (subfaction falls back to parent). Maxed factions with paragon unlocked show **paragon
progress on a darker-faction-colour track**; the value reads green **"paragon"**, swapping to raw
`prog / threshold` on row hover (`LabeledBar.hoverValue`).

**Constructor options:** `expansionLevel` (10=TWW, 11=Midnight; passed to `GetMajorFactionIDs`),
`extraFactionIDs` (IDs the API omits, e.g. Silvermoon Court `2710`, Slayer's Duellum `2770`), `width`.

**Subfaction tiers** (tried in order): (1) `GetMajorFactionData` → renown; (2)
`GetFriendshipReputation` with `maxLevel > 1` → friendship rep (e.g. Valeera `2744`); (3)
`C_Reputation.GetFactionDataByID` → standard standing / `minorFactionMaxStanding[parentID]`.

## SavedVariables (`WarbandeerDB`)

```lua
{ version = 5,
  settings = { defaultView = int, tooltipSide = int, windowPos = { x, y },
               -- per-column Summary visibility; [columnKey] = false hides it
               -- (absent/true = shown, so {} means every column visible)
               summaryColumns = { [columnKey] = bool } },
  -- per-character, per-profession crafting intent: "main" | "secondary" | "gatherer"
  profIntent = { [charName] = { [skillLineID] = intent } },
  -- per-item accepted wrong-enchants; key "<itemID>:<enchantID>" = true means the
  -- (non-recommended) enchant on that specific item is fine — Detail won't flag it
  ignoredEnchants = { ["<itemID>:<enchantID>"] = true } }
```
`MigrateDB`: v1 seeds `settings.defaultView`; v2 adds `profIntent = {}`; v3 seeds `tooltipSide = 1`
(Left); v4 seeds `settings.summaryColumns = {}` (every column visible by default); v5 seeds
`ignoredEnchants = {}`. All migrations are
non-destructive. `windowPos` is not migrated — written lazily at runtime.

## Gotchas

- **Cold-session FontString rasterization glitch.** On the first UI load of a client session (not after `/reload`), cell FontStrings in the Overview's two TableFrames can render blank even though text/size/visibility/font all report correct via the API. A same-params `SetFont`/`SetText` is a client no-op and does NOT heal it; only a *real* font change does. Fix: `healCellFonts` in `Overview.lua` swaps each cell label's font away (FrizQT) and back one tick after construction (`ns:after`). The `/wb cells [heal]` diagnostic in `commands.lua` can probe/heal live if it recurs elsewhere.

- **Don't pass `ns.theme` to free-floating CleanFrames** (e.g. `CharacterTooltip`, parented to UIParent). The theme's `window` token is **alpha 0** — views layer it over the MainWindow surface — so a standalone frame using it as its CleanFrame background turns transparent. MainWindow itself keeps an explicit opaque `background` for the same reason.
- **Known-zero vs doesn't-apply in Summary cells.** Weekly currency columns (crests, field accolades, M+, caches, coffer keys) return the shared muted-em-dash cell `ns.ZeroDash` for a **max-level** character that genuinely holds 0, distinguishing it from a **below-max** character the column doesn't apply to (which stays empty `""`). The gate is `t.basic.level >= ns.wow.maxLevel`. The upgrades column returns `ns.ZeroDash` ungated for its flat n/a (no upgrades) case. `ns.ZeroDash` is a shared constant — safe because `SummaryView:decorateRow` copies every cell before wrapping.
- **Decorated table cells must be shallow-copied.** Several `getData` fns return shared table objects (e.g. `faction.lua` → `ns.icons.AllianceLight`); decorating in place chains hover/click wrappers across every row sharing the object and corrupts it globally. SummaryView/GearView/RaceView copy each cell before wrapping.
- **Row hover/click is cell-driven, not row-driven.** Data cells are mouse-interactive for their per-column tooltips, so a mouse-enabled row would steal those events. `decorateRow` chains onto each cell's `onEnter`/`onLeave`/`onClick`.
- **`LabeledBar` frame height comes from the body font size, not `nameLabel:GetHeight()`** — a FontString reports `GetHeight()==0` until laid out a frame later, leaving the frame too short to cover/receive mouse over the bar.
- **`LabeledBar` hover lightens its own fill+track** in addition to the row `highlight` backing, because the opaque bar (a child frame) occludes the highlight overlay.
- **Faction "done" ≠ `IsFactionParagon`.** Paragon caches exist from the start of Midnight factions. Use `renownLevel == maxLevel` (major) / `reaction >= 8` (standard); only consult paragon *after* max. `GetMajorFactionData`/`GetMajorFactionRenownInfo` return nil for Valeera (2744) despite `IsMajorFaction` — use `GetFriendshipReputation`. Some standard reps (e.g. Slayer's 2770) expose a dummy `friendshipFactionID` with `maxLevel == 1` — guard with `maxLevel > 1`.
- **Dynamic tables need explicit final width/height** — `addCol`/`addRow` accumulate only column/row spans, not header offsets (RaceView, RoleView).
- **Icon TGAs are white and tinted in-game**, rasterized from `@material-symbols/svg-400` (32-bit, descriptor 0x08, matching `bar-rounded.tga`) — see the design repo `iconbuild/`. Regenerate there to add view glyphs.
