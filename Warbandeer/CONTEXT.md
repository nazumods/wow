# Warbandeer (Main UI)

**Deps:** LibNAddOn, LibNUI, Warbandeer_Characters · **OptionalDeps:** Warbandeer_Bars (`bars` view), Warbandeer_Collected (`collected` view), Warbandeer_HousingDecor (`decor` view), ShadowsOfUI-Upgrade (gear-upgrade markers) · **SavedVars:** `WarbandeerDB` (v7) · **Commands:** `/warband`, `/wb` (+ one per view) · **Reads:** `WarbandeerApi`, `WarbandeerBarsApi`, `WarbandeerCollectedApi`, `WarbandeerHousingDecorApi`, `ShadowsOfUI_UpgradeApi` · **UI:** LibNUI

Main viewer UI. Reads the data layer (`ns.api` ← `WarbandeerApi`) and renders it across a set of views switched from a left icon rail.

## Files

| File | Purpose |
|---|---|
| `init.lua` | Addon init (assignment form) + `ns:RegisterSettings` (`defaultView`, `tooltipSide`; the per-column **Summary Columns** subcategory is registered separately in `views/summaryColumnsSettings.lua`, the **Bar Apply Defaults** subcategory in `views/barApplyDefaultsSettings.lua`, and the **Hide minimap button** checkbox in `minimap.lua`). Defines `ns.views`, `ns.viewOrder` (nav order), class/race arrays, the shared `ns.HealCellFonts(tbl)` cold-session font-heal, `MigrateDB` (v4 seeds `settings.summaryColumns`, v7 seeds `settings.barApplyDefaults`), `onLoad`. Calls `ns:RegisterChangelog()` (LibNAddOn) to add the **Changelog** button to the settings category. **Three live refreshers** wired in `onLoad`, each generation-guarded + debounced and gated on the relevant view being the one shown: `PLAYER_EQUIPMENT_CHANGED` → re-runs Detail's `OnBeforeShow` for the logged-in character (`DETAIL_REFRESH_DELAY` 750ms; see DetailView); `PLAYER_ACCOUNT_BANK_TAB_SLOTS_CHANGED`/`BANKFRAME_CLOSED` → re-runs the shown Summary/Gear `OnBeforeShow` (`BANK_REFRESH_DELAY` 2100ms) so a cross-character upgrade marker clears the moment its item is withdrawn from the **warband bank** — the data layer rebuilds `db.bank.warband.equip` on the same events (a remaining duplicate stays listed), and the long delay outlasts both that rescan and ShadowsOfUI-Upgrade's ~2s memo TTL so `OnBeforeShow` re-reads the fresh list, not a cached upgrade result; `PET_STABLE_UPDATE`/`PET_INFO_UPDATE`/`UNIT_PET` → `Refresh()`es the open Detail **Pets/Demons panel** (`PETS_REFRESH_DELAY` 300ms) so a Hunter's active↔stable move / rename or a Warlock's demon summon shows without a `/reload` — the data layer rescans `toon.pets` / `toon.demons` on the same events and loads first, so its handler runs before this one; the debounce just coalesces the burst of moves |
| `changelog.lua` | `ns.changelog` — newest-first `{version, notes}` release history for the in-game **Changelog** viewer (LibNAddOn). **Generated** — `release.sh` prepends each release; not hand-edited |
| `data.lua` | `ns.data` — ilvl/gear-tier color helpers (`IlvlColor`, `IlvlColorObj`), `ns.IlvlTooltipLines(t)` (per-slot ilvl breakdown lines, tier-colored + upgrade-track suffix; shared by the Summary iLvl column and the Overview ilvl cell), faction color/standing override tables, profession-intent helpers (`GetProfIntent`/`SetProfIntent`, `GetMainCrafter`, `GetProfToons`, `FindProf`, `EstimateConcentration`). Also `ns.gearSlots` (draw order), `ns.factionIcon`, and `ns.gearSlotIcon` (per-slot `{path,coords}` into the `icons/gearslots.tga` atlas — a 4×4 grid of 256px cells; Finger1/2 alias the ring cell, Trinket1/2 the trinket cell) |
| `theme.lua` | `ns.theme` — a LibNUI `ui.Theme` ("void-dark"): `colors` + `fonts` (`{path,size}` tuples). Passed on MainWindow, so every widget in the window inherits it; `window`/`border`/`divider`/`text`/`muted`/`header` and fonts `title`/`body` override the LibNUI dark defaults, the rest (`module`, `hover`, `gold`, …) are Warbandeer-specific tokens. **Also published on LibNUI's shared `ui.themes["void-dark"]` registry** so sibling addons can render in the same skin without depending on this addon — the standalone `Warbandeer_Collected` window reads it to match the embedded collected view, falling back to `ui.themes.dark` when this addon isn't loaded (fonts resolve fine since registration only runs once this addon, which ships them, has loaded) |
| `media/fonts/` | Bundled fonts (Hanken Grotesk, Geist, JetBrains Mono) + licenses; loaded by path, not listed in `.toc` |
| `controls/CharacterTooltip.lua` | `ns.CharacterTooltip` + `ns.ShowCharacterTooltip`/`HideCharacterTooltip`; the floating summary card shown when hovering a character cell — name / race / spec + class / realm / **guild** (`<GuildName>`, from `toon.guild`) / level. The guild line is optional: guilded characters show it and the level line sits below it, unguilded characters hide it and the card collapses that reserved space (SetToon re-anchors the level line to the realm line and swaps `_hFull`↔`_hNoGuild`). Side-aware anchoring via `ns.TooltipSide()` / `ns.AnchorTip(frame)` (for `ui.tip`) / `ns.InfoTipPosition(frame)` (LibNUI position spec for the Collected InfoTip; shared by CollectedView + the Overview set cells). Registered on `ns`, not `ui` |
| `controls/ItemTooltip.lua` | `ns.ShowItemTooltip(frame, link, compareLink?, noUpgradeBlock?)` / `ns.HideItemTooltip()` — shared private `WarbandeerItemTooltip` (GameTooltipTemplate) reskinned (`styleTooltip` → flat black + theme border via `NineSlice`) that `:SetHyperlink`s an item link; WoW auto-comparison off. `compareLink` puts the suggested upgrade on the *anchored* tip (closest to the row/cursor, like the item under consideration in a normal comparison) and shows the equipped item on a second private tooltip (`WarbandeerItemCompareTooltip`) extending outward beside it (away from the row per `TooltipSide`), carrying an integrated "Equipped" tab. Tooltips use a dark near-black `BORDER` (the theme tan reads too bright here). Detail uses this to lay the upgrade next to the equipped item. `noUpgradeBlock` sets `tooltip.SkipUpgradeBlock` so ShadowsOfUI-Upgrade's "Upgrade for:" line is suppressed (compare tooltip always opts out). Used by Detail + Gear gear cells; anchor side per `ns.TooltipSide()`. `ns.ShowEnchantTooltip(frame, suggestion)` reuses the same `itemTip` frame for a focused **recommended-enchant** tooltip (Detail's "Missing enchant" note hover): a gold name header + a green wrapped **effect description** of what the enchant grants (via `enchantText` → `GetSpellDescription` for a spell id, or `GetItemSpell` → its use-spell description for an item id) — deliberately *not* the scroll/recipe's full item tooltip (reagents/sell price). `ns.ShowGemTooltip(frame, suggestion)` is the gem sibling (Detail's "Empty socket" note hover): a gem has no use-spell, so it just `:SetItemByID`s the gem (its own short item tooltip carries the stat bonus = what it does). Both hidden by `ns.HideItemTooltip` |
| `controls/StatCard.lua` | `ns.StatCard` — summary tile (caption + big mono `amount`, optional `sub` + trend `subIcon`). `Amount(text, color?)` |
| `controls/StatChart.lua` | `ns.StatChart` — compact secondary-stat radar (outline only, no fill). Four vertices along the diagonals — Crit (TL), Haste (BL), Versatility (BR), Mastery (TR) — joined into a quadrilateral over a faint square guide. Each vertex's distance is the stat's **fulfilment vs its Archon target** — 1.0 lands on the guide square, capped at `overshoot`× (default 1.2, which reaches the frame corner) so an over-target stat pokes a little past the guide — so under-target stats pull toward the centre and met/over ones reach or pass the guide (matching the red/blue target cells beside it); when a stat has no target it falls back to its rating relative to the largest of the four (balance plot). Edges are WoW native frame lines (`CreateLine`), vertex dots are nine-sliced `bar-rounded` textures; `Set(crit, haste, mastery, vers, hot?, targets?)` re-plots and tints any `hot` (tier-1) stat's dot gold (all-zero hides the polygon). Re-derived from the idea behind Narcissus's radar chart, not its mask-texture implementation |
| `controls/IconStrip.lua` | `ns.IconStrip` — floating left nav rail, one tinted glyph per view; `onSelect(name)`, `SetActive(name)`. The top **brand-mark logo** is a button (`logoButton`): hover shows a "Right-click for menu" hint, and **right-click pops the shared Settings + views menu** (`MenuUtil.CreateContextMenu` → `ns.populateViewMenu`, the same menu the minimap button / addon-compartment offer — so Settings is reachable straight from the open window) |
| `controls/UpgradeMark.lua` | `ns.UpgradeMark(charName, slot)` → cell-text ▲ glyph (green held / gold better-in-warband), `ns.UpgradeTip(charName, slot)` → hover line, `ns.UpgradeSuggestion(charName, slot)` → `link, ilvlGain, warband` for the upgrade item (drives Detail's per-slot sub-line), `ns.MissingEnchantSlots(charName)` → `{ [slot]=true }` set of equipped slots missing a permanent enchant (built once per Detail render, drives the gear-row "Missing enchant" note), `ns.RecommendedEnchant(charName, slot)` → the recommended enchant's **name** (resolves the API's `EnchantSuggestion` via the shared `suggestionName`: its own `name` from ClassCodex, else the id via `C_Spell.GetSpellName`/`C_Item.GetItemInfo`; Detail note + Summary "Ench" hover), `ns.RecommendedEnchantInfo(charName, slot)` → the **raw** `EnchantSuggestion` (kind/id/name, not just the name) so a consumer can render the enchant's own item/spell tooltip (Detail's "Missing enchant" hover → `ns.ShowEnchantTooltip`), `ns.EmptySocketSlots(charName)` → `{ [slot]=count }` of equipped slots with empty gem sockets (Detail note + Summary "Gem" hover), and `ns.RecommendedGems(charName)` → `primary, secondary` recommended gem **names** (the unique diamond + the repeatable fill gem; same resolver, either may be nil), and `ns.RecommendedGemsInfo(charName)` → the **raw** `primary, secondary` gem suggestions (kind/id, for the Detail "Empty socket" hover → `ns.ShowGemTooltip`); all via `ShadowsOfUI_UpgradeApi` (OptionalDep); no-op "" / nil / empty-set when that addon is absent. Also the **wrong-enchant** trio: `ns.EnchantMismatchSlots(charName)` → `{ [slot] = {applied, recommended, itemID, enchantID} }` (the RAW mismatch set, *unfiltered* by the accept list, so the view can still offer an un-accept on an accepted row), plus the per-item accept toggle `ns.IsEnchantIgnored(itemID, enchantID)` / `ns.ToggleEnchantIgnore(itemID, enchantID)` backed by `WarbandeerDB.ignoredEnchants` (key `"<itemID>:<enchantID>"`, account-wide) |
| `controls/SuggestedBox.lua` | `ns.SuggestedBox` — Detail's left-column "SUGGESTED" panel: the highest-impact gear actions for the character, merged across every source and ordered by ilvl gain. `Populate(char)` (caller sets width first) gathers candidates from `ShadowsOfUI_UpgradeApi:CharacterUpgrades` (ready gear, held green / warband gold), `:WorldQuestUpgrades` (orange) and `:VendorUpgrades` (faction-quartermaster, cyan), drops items whose required level exceeds the character's, then **keeps the single biggest upgrade per slot** (a better vendor piece replaces a smaller bank/WQ one) and **sorts by ilvl gain desc** — so the most impactful action leads regardless of source. Ties break toward the cheapest action (held/warband > WQ > vendor). Fills ≤5 pooled **two-line** rows: a headline `{icon} [Item] +N ilvl` (gain tinted by source) over a smaller muted `bodySmall` sub-line spelling out the source — the slot (`Main Hand`) for ready gear, `WQ: <title> — <zone>`, or `Buy: <quartermaster> (<zone>) — <cost>` — so the detail gets the row's full width instead of truncating. Row hover lays the upgrade beside the equipped piece via `ns.ShowItemTooltip`. **Clicking a WQ row opens the world map to the quest's zone (`mapID`) and super-tracks it; a vendor row opens the map to the quartermaster's zone** (no quest to super-track); a **ready row (held/warband) flashes the item across the open bags/bank** (`ns.FlashItem` on `_compareLink`/`_itemLink`). The WQ + vendor sources degrade silently on an older Upgrade addon without the respective method. Shows "Nothing pressing — looking good!" when there are none; hides + returns 0 height when ShadowsOfUI-Upgrade (OptionalDep) is absent |
| `controls/ConsumablesBox.lua` | `ns.ConsumablesBox` — Detail's right-column "CONSUMABLES" panel (beneath the gear panel): the logged-in spec's recommended **flask / combat potion / food / weapon buff / augment rune**, read from `ShadowsOfUI_UpgradeApi:RecommendedConsumables` (ClassCodex via ShadowsOfUI-Upgrade, OptionalDep — no bundled fallback). `Populate(char)` (caller sets width first) lists one pooled row per category — a muted category label + the item (inline icon + rarity-coloured link, `itemDisplay` resolves a cached link or shows the CC name + requests a load) — hovering shows the item's tooltip (`ns.ShowItemTooltip`). Defines `ns.ConsumableCats` (the `{key,label}` order, shared with `consumablesSettings.lua`); a category with `db.settings.consumables[key] == false` is skipped. **Hidden + zero-height** when the master toggle is off (`db.settings.showConsumables == false`), the upgrade addon / ClassCodex is absent, the spec has no consumables data, or every category is toggled off. Modelled on `SuggestedBox`. |
| `controls/GlyphBox.lua` | `ns.GlyphBox` — Detail's right-column appearance box (beneath ConsumablesBox), `Populate(char)` → height. Up to four sections, **top to bottom: Class Mounts → Appearance Unlocks → learned unlocks → Appearance Glyphs**. **"APPEARANCE GLYPHS"** — the *applied* cosmetic glyphs for a **selected spec** (`WarbandeerApi:GetAppliedGlyphs(char, specID)`), rendered as a labeled **list** (glyph items share one generic tome icon, so names carry the meaning; applied = green, unapplied muted) with a **spec-icon picker** above it (`classSpecs` via `GetSpecializationInfoForClassID`; defaults to the char's active spec, remembers the pick until the subject changes; selected = gold border, a spec with captured data normal, one never scanned dimmed; hover → spec name; click fires `onSpecChange`, which the host wires to re-run `OnBeforeShow`+`Fit`). Header shows `owned / N`, or `? / N` when that spec was never scanned (unknown ≠ none). **learned-unlock section** (header = the class's own title via `GetLearnedUnlocks` — **"TOME OF THE WILDS"** for Druid, **"SKILL TAMES"** for Hunter) — the character's *learned* per-character class unlocks (items/skills that permanently grant an ability; the Hunter set folds in innate racial grants like Goblin/Gnome Mechanical taming), a labeled **list** (known = green, missing muted). **"APPEARANCE UNLOCKS"** — account-wide Druid Marks/travel glyphs + Warlock demon Grimoires + green fire (progressive; shows the next step until earned) (`GetAppearanceUnlocks`), an **icon grid** (distinct icons; owned full-colour + gold border, missing dimmed). **"CLASS MOUNTS"** — the class's Legion order-hall mounts + spec-gated colour tints as account-wide collectibles (`GetClassMounts`), a labeled **list** (tint recolors share a model, so names carry the meaning; owned = green + full-colour icon, missing muted + dimmed); DK is one entry whose colour follows the active spec (not three). The three list sections (mounts, learned, applied glyphs) share the pooled `_row` + a `listRow()` fill helper. Every entry hovers → `ns.ShowItemTooltip` (mount rows → `ns.ShowMountTooltip`, which renders via `GameTooltip:SetMountBySpellID`); an **unowned/unapplied** entry appends a muted "How to obtain" line to that tooltip (the API entry's `source` — from `Warbandeer_Characters/data/collectiblesources.lua` for glyph/unlock/learned rows, inline on the catalog entry for mounts — passed as the tooltip's `obtain` arg; owned entries suppress it). Hidden + zero-height for a class with none of these systems (e.g. Evoker). Modelled on `ConsumablesBox`/`SuggestedBox` |
| `controls/PetsPanel.lua` | `ns.PetsPanel` — a Hunter's pet roster in a **companion panel docked to the right of the window** (a big stable is far too large to inline on Detail), modelled on the Bars view's preview/apply companions: a `CleanFrame` parented to the DetailView (so it hides when Detail is left / the window closes), anchored to the main window's right edge **top-to-bottom** (`TopRight`+`BottomRight`, so it spans and tracks the window height), with an **explicit opaque `background`** (the theme `window` token is alpha 0, so a themed CleanFrame would render transparent — MainWindow does the same). A **titlebar strip** matching the main window's (the shared `"titlebar"` colour token + the `title` font, so the two read as one piece) with an invisible `_dragStrip` grip **bound to `ns.MainWindow:BindDragHandle`** so dragging it moves the whole window (like the Bars preview heading) sits above one `ui.VirtualList` (themed auto-hiding scrollbar) of two row types: **section headers** (a caps label + a right-aligned count — ACTIVE PETS / STABLE — over a full-width accent underline rule that demarks the sections) and **pet rows** (`IconListItem` — family icon + name + muted `<family>` suffix + a `Lv N · <spec>` sub-line where **N is the character's level** — pets scale to the hunter, so the stored per-pet `PetInfo.level` is a stale last-active value and the character's level is shown instead; exotic families noted; each pet row also **hovers a `tooltip`** — `IconListItem`'s built-in `ui.tip` hover fed a `tooltip` list — restating name/family/spec/level/exotic, plus the fold count on a collapsed `×N` row). Pets that would render identically (same name/family/spec/exotic — e.g. the many collected skins of one artifact pet like Hati, all "Hati • Spirit Beast") collapse via `collapsePets` into a single **`×N`** row so a large stable stays readable (the section-header count stays the true total, so the rows still account for every pet). Fed from `WarbandeerApi:GetPets`. The curated Challenge-Tames checklist is **not** inline here — it lives in the sibling `ChallengeTamesPanel` docked further right. **Class-aware:** for a **Warlock** (the other pet class, with no stable) `Set` instead titles the panel "Demons — <name>", swaps the list's `emptyText` (via `VirtualList:EmptyText`) and renders one "DEMONS" section from `WarbandeerApi:GetDemons` using a `demon` row type (a shorter `IconListItem`: a **per-species icon** — a module-level `npcID → summon-spell-icon-path` map with a generic fel fallback — and a `name • species` title, no sub-line since a demon has no level/spec; the row likewise **hovers a `tooltip`** of name, species and last-seen date). An empty roster (a Hunter who hasn't visited a stable, or a Warlock that hasn't summoned) falls through to the list's `emptyText`. `Toggle(char)` (show/hide) is wired to DetailView's Hunter/Warlock "Pets"/"Demons" button; `IsOpen()` reports its shown state so DetailView keeps the sibling tames panel in step; `Refresh(char?)` re-renders an already-open panel — with a `char` when the Detail subject changes, or no arg to re-read the current subject on a live pet change (wired in `init.lua` to `PET_STABLE_UPDATE`/`PET_INFO_UPDATE`/`UNIT_PET`) |
| `controls/ChallengeTamesPanel.lua` | `ns.ChallengeTamesPanel` — the Hunter Challenge-Tames owned/missing checklist in its **own** companion panel, docked to the **right of the pets panel** (`PetsPanel`) so it isn't buried beneath a large stable. Same chrome as `PetsPanel` (a `CleanFrame` with an explicit opaque `background`, a `"titlebar"`/`title` strip whose `_dragStrip` grip moves the whole window via `ns.MainWindow:BindDragHandle`, one `ui.VirtualList`), with two row types: **category headers** (SPIRIT BEASTS / MOLTEN FRONT / MECHANICAL — a caps label + a right-aligned **owned / total** count over an underline rule) and **tame rows** (a beast icon + the tame's name + a sub-line, tinted by owned state — owned shows the matched pet's family icon at full colour with a green name + an "as <your pet's name>" sub-line, missing a dim generic tame icon with a muted name + the tame's zone note, mirroring the GlyphBox owned/missing look). Each tame row **hovers to a tooltip** (`ui.tip`) — the tame name plus, for a missing tame, a gold "How to obtain" header + the full `howTo` detail (spawn spot, taming mechanic, prerequisites), or "Owned as <pet>" when owned; the inline sub-line is a non-wrapping 260px strip, too cramped for the how-to. `Set(char)` groups `WarbandeerApi:GetChallengeTames(char)` into per-category sections and shows the panel for any Hunter (all-missing before a stable visit); `IsOpen()`/`Refresh(char?)` mirror `PetsPanel`. **Hunter-only** — no other class has a tameable roster to match, so DetailView only opens it for Hunters, kept in the same open/closed state as the pets panel via the Pets button + `OnBeforeShow` |
| `controls/FlashItem.lua` | `ns.FlashItem(link)` — "flash find": pulse every on-screen bag/bank slot holding the item (matched by itemID), so clicking a suggested upgrade locates it. **Detect-and-adapt**: fires Bagnon's own `FLASH_ITEM` signal (`LibStub("WildAddon-1.1", true).Calls:Fire("Bagnon.FLASH_ITEM", id)`, guarded — Bagnon replaces the default bag UI so its frames must pulse natively; the WildAddon signal is tagged by the **front-end** addon owning the live buttons — `Bagnon.` — not the shared `BagBrother` engine) **and** walks Blizzard's default container frames (`ContainerFrameUtil_EnumerateContainerFrames` → `flashBlizzard`) + the `BankPanel` item-button pool, playing a private additive border-glow overlay on each match (Blizzard's built-in `flashAnim` can't be reused — the new-item OnUpdate stops it next frame for a non-new item). Opens the bags first (`OpenAllBags`); a bank-only item flashes only while a banker is open. Wired to Detail gear-row clicks (`ns.detail.attachItemTip` → `_upgradeLink`) and Suggested-box ready-row clicks (`_compareLink`/`_itemLink`) |
| `controls/LabeledBar.lua` | `ns.LabeledBar` — progress row (name + value + bar beneath). Setters `Fill`/`Label`/`Value`/`BarColor`; optional `onClick`, `onEnter`/`onLeave` (hover callbacks, e.g. a tooltip — used by the Overview Houses strip), `hoverValue`/`hoverColor` |
| `media/bar-rounded.tga` | 16×16 white rounded-rect, nine-sliced as the `LabeledBar` bar texture; tinted at runtime |
| `icons/views/*.tga` | White per-view glyphs (+ `logo.tga`) tinted in-game by `IconStrip` |
| `icons/pip-full.tga` / `icons/pip-empty.tga` | Gold filled disc / muted ring — the Great Vault view's unlocked / locked slot pips, drawn inline in a cell via `\|T…\|t` markup (colour baked in). Regenerate all three vault assets with `Tooling/make_vault_assets.py` |
| `icons/lfgEye.tga` | Group Finder eye glyph (white 64×64, muted-tinted in-game) — the Great Vault view's **Dungeons column** header. Sourced from the `groupfinder-eye-single` UI **atlas element** (not an icon-CDN icon): fetched from `wow.zamimg.com/images/wow/TextureAtlas/live/groupfinder-eye-single.png`, then run through `Tooling/make_summary_icon.py --src … --alpha luma` |
| `icons/raidDragon.tga` | Dragon-head glyph (white 64×64, muted-tinted in-game) — the Great Vault view's **Raid column** header. From the `inv_misc_head_dragon_01` game icon via `Tooling/make_summary_icon.py --icon-name inv_misc_head_dragon_01 --floor 0.12 --gamma 0.55` (tuned darker/higher-contrast so the dark dragon art reads as a silhouette) |
| `icons/endeavor-{alliance,horde}{,-sub}.tga` / `icons/endeavors.tga` | Endeavors art (#550): baked-colour faction crests — plain in the Overview house blocks, gold-ringed `-sub` in the Summary cell (the endeavor being fed) — plus the white carpenter's-hammer column header (muted-tinted). Regenerate with `Tooling/make_endeavor_assets.py` |
| `icons/trending_{up,down}.tga` | White up/down trend arrows for the Overview weekly-gold card |
| `icons/*.tga` | White 64×64 glyphs for Summary column headers (`crest_hero`, `crest_myth`, `catalyst`, …), tinted muted in-game |
| `views/Overview.lua` (+ `views/overview/`) | **Split across files**, sharing its sub-widgets via an `ns.overview` table (mirrors `ns.detail`): `overview/FactionBars.lua` (the rep-bar stack + faction helpers), `overview/TopAlts.lua` + `overview/TopAltsMarks.lua` (the Top Characters gear table — data/lifecycle vs the rating-overlay marks), `overview/Achievements.lua` (the per-expansion checklist widget + the achievement-ID lists). The `Overview.lua` core is the view shell: stat strip, three-column layout, `capsHeader`, `buildTab`, `EXPANSIONS`, the cold-session heal (shared `ns.HealCellFonts`), and `BuildFilter`. An account-wide **Traveler's Log** strip (Trading Post monthly activity) sits between the stat strip and the grid when data is present — a `capsHeader` + a full-width `LabeledBar` (`pct`, raw `earned/max` on hover) with the current Trader's Tender + reset countdown on the right, and a gold rewards-waiting line beneath (bag glyph + pending Tender + reward item icons; muted "all collected" when none). It reads `WarbandeerApi:GetTravelersLog` and offsets the grid's `contentTop` by its height so panels / Top Characters / module backgrounds shift down together (no snapshot ⇒ no strip, no offset). A second account-wide **Houses** strip (endeavors, #550) stacks below the Traveler's Log strip when a house is captured: two faction-house blocks (Alliance | Horde), each an `icons/endeavor-{faction}.tga` crest + `name · Lvl N · favor XP` line over the current endeavor's progress `LabeledBar` (faction-tinted; its `onEnter` tooltip surfaces the still-earnable House XP + neighborhood progress + reset). Reads `WarbandeerApi:GetHouses` and offsets `contentTop` like the Traveler's Log strip (the two stack via `tlBlock` + a houses block). The `overview/*` files load **before** `Overview.lua` in the toc. Stat strip + Reputations / Achievements / Top Characters modules; titlebar `BuildFilter` carries a raid picker + an expansion picker. Rep bars via `FactionBars` (see below); rows click through to Detail. The `TopAlts` table appends one column per raid difficulty (RF/N/H/M) showing the selected raid's per-class transmog-set completion — uncollected piece count (red→green shaded) or a green check — read from `WarbandeerCollectedApi` (OptionalDep) via `SetStatus`; `RAIDS`/`DIFFS` drive the raid dropdown + columns, `TopAlts:SetRaid`/`Refresh` re-render, and `Overview:OnBeforeShow` refreshes on each open. **Cell tooltips** (matching Summary/Collected): the ilvl cell hovers a per-slot ilvl breakdown (`ns.IlvlTooltipLines` → `ui.tip`; click → Gear) and each set cell hovers the shared Collected InfoTip (`ShowInfoTip`, anchored via `ns.InfoTipPosition`; left-click → `ShowDressingRoom`, **Shift-click → `ToggleWanted`**). Hover/click is **cell-driven, not row-driven** — a mouse-enabled row layers above the cells and swallows their tooltips/clicks, so (like `SummaryView:decorateRow`) GetData's `decorate()` chains the row highlight (`TopAlts:_hover`) onto every cell's hover and defaults its click to `_openDetail` (→ Detail) unless the cell carries its own (ilvl → Gear, set → dressing room / shift-toggle wanted); the row itself is left non-interactive. **Rating overlays** mirror the `/collected` `DataView` grid (Overview reimplements them on its own `TopAlts` table — it's not the shared grid): every set cell carries its `setId`, and `TopAlts:_refreshMarks`/`_applyCellMarks` (run by an `update` override + a ctor seed, since the constructor-time base `update` predates the override) draw the gold wanted star (top-left) + the tier letter in its tier color (top-right) from live `WarbandeerCollectedApi` state, resolved against the logged-in char's `_playerRace`; a Shift-click re-applies the toggled cell's marks directly, and a module-level `WarbandeerCollectedApi:OnRatingsChanged` listener re-marks the table live when a rating is edited in the shared dressing room |
| `views/SummaryColumns.lua` | `SummaryColumn` specs (`getData`/`getFooter` per column; each toggleable column carries a stable `key` + settings `label`, identity columns have neither) + `ns.VisibleSummaryColumns()` (the column list filtered by `db.settings.summaryColumns[key]`, identity columns always kept) + `SummaryColumnsDelayed()` (appends the dynamic DMF column to a table's `_columns`/`addCol` while the faire is open — **not** inserted into the global `ns.SummaryColumns`, so it's always-shown-while-active and never appears in the settings panel) |
| `views/summaryColumnsSettings.lua` | Builds the **Summary Columns** settings subcategory (nested under "Warbandeer"): one checkbox per toggleable column (those with a `key`), default on, writing `db.settings.summaryColumns[key]` and calling `ns.RefreshSummaryColumns()` on change. Loads after every `summaryCol/*.lua` so `ns.SummaryColumns` is complete (gated Up/Ench/Gem checkboxes appear only when ShadowsOfUI-Upgrade is loaded) |
| `views/VaultColumns.lua` | `ns.VaultColumns` — the Great Vault view's column set (built from `ns.SummaryColumn`, a **separate registry** from `ns.SummaryColumns`): faction crest, name (+ unclaimed-vault marker), level, Raid / Dungeons / World slot pips, keystone, raid lock. The Raid (`raidDragon` dragon head) and Dungeons (`lfgEye` Group Finder eye; the vault's Heroic/Mythic/Timewalking track, not M+-only) column headers are icon glyphs (`iconPath`) rather than text; World + the rest use text headers. The five data columns each carry a stable `key` + settings `label` (crest / name / level are identity columns with neither). Pip markup + per-slot tooltips live here + `ns.VisibleVaultColumns()` (the column list filtered by `db.settings.vaultColumns[key]`, identity columns always kept — the Vault twin of `ns.VisibleSummaryColumns`) |
| `views/VaultRoster.lua` | `ns.VaultRoster` — one faction's Great Vault table (`TableFrame` subclass): roster filter (faction **+ max-level only**, since the Great Vault is a max-level feature) + `byLevelIlvl` sort, row hover / click-to-Detail, logged-in gold wash. Renders the visible `columns` list passed in by `VaultView` (not the global `ns.VaultColumns`), so a hidden column drops out on rebuild. `VaultView` builds three (Alliance / Horde / Both), each from `ns.VisibleVaultColumns()` (resolved once per build) via `buildColInfo(cols)` |
| `views/vaultColumnsSettings.lua` | Builds the **Great Vault Columns** settings subcategory (nested under "Warbandeer"): one checkbox per toggleable Vault column (those with a `key`), default on, writing `db.settings.vaultColumns[key]` (the `table` closure ensure-creates it) and calling `ns.RefreshVaultColumns()` on change. Loads after `VaultColumns.lua` so `ns.VaultColumns` is complete. The Vault twin of `summaryColumnsSettings.lua` |
| `views/consumablesSettings.lua` | Builds the **Consumables** settings subcategory (nested under "Warbandeer"): a master **Show consumables** checkbox (`db.settings.showConsumables`, default on — hides the whole box when off) followed by one checkbox per `ns.ConsumableCats` entry (Flask / Combat Potion / Food / Weapon Buff / Augment Rune), default on, writing `db.settings.consumables[key]` (the `table` closure ensure-creates it) and re-rendering the open Detail view on change. **Early-returns (registers nothing) unless `ShadowsOfUI_UpgradeApi:RecommendedConsumables` exists**, since consumables are a ClassCodex-only surface — like the gated Up/Ench/Gem columns. Loads after `controls/ConsumablesBox.lua` (which defines `ns.ConsumableCats`) |
| `views/summaryCol/*.lua` | One file per Summary column: faction, role, character, level, ilvl, upgrades, enchants, gems, catchup, profs, bags, mail, auctions, vault, keystone, crests, catalyst, voidcore, manacrystal, dundun, delves, delversBounty, lumber, cofferKey, fieldaccolade, unalloyedabundance, caches, xp, rested, endeavors, played, titles, gold. **`profs.lua`** keeps the gear-% scoring + column registration but splits its warband craft-hint machinery (the who-can-craft/upgrade index + per-item/empty-slot hint lines) into **`profsCraft.lua`** (loaded first, exposed on `ns.profsCol`). `titles.lua` (the "Titles" column — plain text header, `autosize = true`) shows each character's **current (featured) title** via `t.titles.currentName` (`WarbandeerApi:GetTitles`); the column sizes to the widest title currently shown (`SummaryColumn` threads `autosize` into its `colInfo`; `ClassSummary:AutosizeColumn` then measures the widest header/cell text (floored at an optional `minWidth`) and grows that column + the table by the delta — **not** `TableFrame:Autosize`, whose width recompute is a plain column-width sum that drops this view's per-column gutters and spills the last column off the window edge. Cells + footer cells anchor to their column, so the resize reflows them and every later column, and `SummaryView:layout` re-fits the scroll + window). `ClassSummary` sets `detachedFooter = true` (LibNUI `TableFrame`), so footer totals are sized to their own content and anchored to their column by one edge instead of spanning it — a wide aggregate (e.g. the g/s/c gold total) no longer clamps its column's width, leaving any data column free to autosize below its total (Gold: `autosize` + `minWidth = 90`, so it collapses to its gold cells while the wide total overflows left — the floor keeps that total clear of the Played total when the Titles buffer column is shown) and hovering shows the character's earned-title count. Blank for an alt not seen since the titles broker landed or with no title selected; toggleable (`key = "titles"`). (The full earned/earnable title browser is a separate Titles view — a follow-up.) `endeavors.lua` (the "Endeavors" column — muted carpenter's-hammer glyph header `icons/endeavors.tga`, `width 30`, ordered just before Played) shows the faction crest of the neighborhood endeavor each character is **actively feeding** right now (a gold-ringed `icons/endeavor-{faction}-sub.tga` via `WarbandeerApi:GetHousing` → `active`), blank when it's in none; hover shows the endeavor title + which house. Toggleable (`key = "endeavors"`). The house-XP totals are account-wide, so they live in the Overview, not per row. `delversBounty.lua` (the Bounty column — muted treasure-map glyph header `icons/delversBounty.tga`, generated from the `icon_treasuremap` game icon via `Tooling/make_summary_icon.py` to match the actual Delver's Bounty treasure-map item) shows a green check once a character has claimed the weekly Delver's Bounty treasure (`t.weeklies.delversBounty`, quest 86371), a muted em-dash for a max-level character who hasn't, blank below max. `keystone.lua` (the "M+" column — right-justified) shows the owned keystone level (`+N`, `t.weeklies.keystone`), a muted em-dash at max level holding none, blank below max; hover names the dungeon (`t.weeklies.keystoneMap` → `C_ChallengeMode.GetMapUIInfo`) + level, falling back to level-only when the map is absent (older data / an alt not seen since v41). `auctions.lua` (the "AH" column — plain text header) shows each character's active-auction count via `t.auctions`, **red** when the soonest expires within 6h; the count is **live** (derived from stored expiry stamps still in the future), so a stale cache (auctions run ≤48h) or expired auctions read as blank rather than a phantom number. `mail.lua` (the "Mail" column — plain text header, no icon) reflects each character's mailbox via `t.mail`, **red** when the soonest piece expires within 3 days. **New (unread) mail waiting trumps the count**: when `t.mail.hasMail` is set the cell shows *only* an inline minimap mail envelope (`ui-hud-minimap-mail-up`) — the `HasNewMail()` minimap-envelope state cached by the data layer (`Warbandeer_Characters/data/mail.lua`, on `UPDATE_PENDING_MAIL` + each inbox scan), so it persists across `/reload` and shows on alts. With no mail waiting it falls back to the cached item/gold count (blank when the mailbox is empty or unscanned). Hover explains. Each toggleable column passes a `key` + `label`; the five identity columns (faction, role, character, level, ilvl) pass neither and are always shown. `manacrystal.lua` (Untainted Mana-Crystals, currency 3356) shows the held count, red when the 250 weekly-earn cap is reached, hover = earned/cap; muted `crystal` glyph header (`icons/manacrystal.tga`). `dundun.lua` (Shard of Dundun, currency 3376) shows the held count, red when done for the week — either full (held 8) or the week's 8 already earned (`capped = held >= maxQuantity or earned >= maxWeeklyQuantity`, both caps 8); hover shows held + earned-this-week; muted crystal-shard glyph header (`icons/dundun.tga`). `fieldaccolade.lua` (Field Accolade, currency 3405) and `unalloyedabundance.lua` (Unalloyed Abundance, currency 3377 — earned from Abundant Harvests at the Abundance event) each show a plain running total (no weekly cap), a muted em-dash at max level holding 0, blank below max; muted white-silhouette glyph headers (`icons/fieldaccolade.tga`, `icons/unalloyedabundance.tga`, the latter traced from the game icon via `Tooling/make_summary_icon.py`). `catalyst.lua` (the "Catalyst" column) shows, for **max-level** characters, a per-character ✓/✗ for "Midnight Season 1: Catalyst Unbound" (achievement 61519, read from `quests.CatalystUnbound`) as an inline atlas immediately left of the Dawnlight Manaflux charge count (red when full); the whole group is **right-justified** so every count — max-level and leveling alike — lines up on the same right edge (leveling rows substitute a same-width transparent `icons/spacer.tga` for the icon, since a leading inline of a different width shifts the right-justified text), and the group sits clear of the column to the left. Hover details earned-state + charges. Leveling alts show only the charge count (the achievement is endgame). The achievement's `completed` is account-wide but `wasEarnedByMe` is per-character, so it's captured per character by the data layer rather than read live. `catchup.lua` (the "<180" column) counts Head/Neck/Hands/Waist slots below ilvl 180 for level-81+ characters (muted em-dash below 81; green check at 0). `xp.lua` shows current XP as a percent of the level's total (hidden at max level). `upgrades.lua` (the "Up" count column — gold by default, **green** when at least one listed upgrade is at-or-below the character's level, i.e. equippable right now regardless of location, a warband-bank copy can be withdrawn; stays gold only when every upgrade is still gated above their level. The per-character upgrade scan (bags+bank+warband candidate evaluation) is **too heavy to run for every character during a synchronous render** — done inline it trips the "script ran too long" watchdog when the window is opened from a secure path (action button / macro → `UseAction`, a tighter budget). So this column computes **off the render frame**: a cold cell returns a muted `…` placeholder and enqueues a compute, a pump processes **one character per tick**, and the open Summary view refreshes once (`ShownView()` + `OnBeforeShow` + `Fit`) when the queue drains. Payloads are cached per character and reused until the data layer rescans that character — keyed on `lastRefresh`, so a stale cache after a gear/bank change recomputes) and `enchants.lua` (the "Ench" column — count of equipped slots missing a permanent enchant, green check at 0, muted em-dash below max level; hover lists slot + item + the recommended enchant via `ns.RecommendedEnchant`, click → Detail), and `gems.lua` (the "Gem" column — total empty gem sockets across equipped gear via `MissingGems`, green check at 0, em-dash below max; hover lists slots + the recommended gems via `ns.RecommendedGems` — the unique diamond "×1" then the fill gem) all early-return unless `ShadowsOfUI_UpgradeApi` exists, so those columns are only registered when ShadowsOfUI-Upgrade is loaded |
| `views/SummaryView.lua` | Three `ClassSummary` tables — `alliance` / `horde` / `both` — one shown at a time, switched by a 3-segment faction `BuildFilter` (`FACTION_SEGS`; joined bordered segments modelled on GearView's armor strip, active segment tinted its faction accent — Alliance blue / Horde red / Both gold). `ClassSummary.faction` ("alliance"|"horde"|"both") drives `GetCharacters` (a single side filters by `isAlliance`; **both** returns the whole warband unfiltered, so footer totals become warband-wide). `_faction` holds the current mode (defaults to the logged-in character's side); `setFaction(mode)`/`_activeTable()` swap the shown table (the newly-shown table is `OnBeforeShow`-refreshed on switch, and only the active table is refreshed each show). Cells drive row hover + click-to-Detail. Each table is built from `ns.VisibleSummaryColumns()` (resolved fresh per build, so a column toggle is reflected on rebuild) via the `makeTable` helper; `buildColInfo(cols)` derives the header colInfo and each table stores its own `_columns` list (row builders walk it; the dynamic DMF column is appended to it). Each table's **row area only** is the scroll child of its own themed `ui.ScrollFrame` (`_scrolls[faction]`), so the header row + totals footer stay pinned while the rows scroll (the footer is re-anchored below the scroll viewport in `makeTable`; table + scroll are siblings, so `layout()` toggles both per faction — mirrors CollectedView's `rowArea` scroll). `layout()` caps the whole view (header + rows + footer) at `maxTableHeight()` (~95% of `UIParent:GetHeight()`, less window chrome — computed live so it tracks resolution / UI-scale); a tall roster (big warband, or the merged **both** list) scrolls its rows instead of growing the window off-screen, shorter ones show in full with the scrollbar auto-hidden (the `SCROLLBAR_W` gutter is reserved only while scrolling) |
| `views/GearView.lua` | Four armor-type tables toggled by `BuildFilter` buttons; per-equipment-slot ilvl + upgrade-track columns. Each slot cell hovers to the shared item tooltip (`ns.ShowItemTooltip`) and appends `ns.UpgradeMark(toon.name, slot)` (the ▲ available-upgrade glyph) |
| `views/DetailView.lua` | Single-character detail (class def + constructor + `Select`/`OnNavigate`/`OnBeforeShow`; the split-out helpers below re-open `ns.views.DetailView`). Header: a faction icon stacked over a role icon, then a gender-aware race icon (`ns.detail.raceAtlas` → `raceicon128-<race>-<sex>` atlas, with a `RACE_ATLAS_FIX` table for race-file tokens that differ from the atlas spelling), then the class portrait, then the identity text (name over a `race + active-spec + class` subtitle — e.g. "Human Shadow Priest"; the spec drops out for an alt scanned before it was captured). Below: stat strip (Item Level + Playtime cards), a **secondary-stat 2×2 grid** (`detail/Stats.lua` → `_showStats`, slotted at `D.STATS_TOP` between the cards and the professions block — `D.PROF_HEADER_Y` is offset by `D.STATS_H` to make room), per-profession intent panels (each followed by that profession's equipped tool/accessory list), equipped-gear list. Gear + prof-gear rows highlight and show the shared item tooltip on hover (`ns.detail.attachItemTip` → row `hover` tint + `ns.ShowItemTooltip`, always with `noUpgradeBlock` so the "Upgrade for:" line is suppressed; see `controls/ItemTooltip.lua`); when an upgrade exists, equipped-gear rows show the suggested upgrade (`ns.UpgradeSuggestion`) on a smaller sub-line beneath the item name, rendered as the item link's own appearance (`[Name]` in rarity colour) + the ilvl gain (tinted green held / gold warband) + `@ lvl N` in red when the item needs a level this character hasn't reached (`ns.detail.reqLevel` via `GetItemInfo`) — no ▲ arrows. Below the upgrade, a slot **missing its permanent enchant** (`ns.MissingEnchantSlots`, precomputed per render into `self._missingEnch`), a **wrong (non-recommended) enchant** (`ns.EnchantMismatchSlots` → `self._enchMismatch`, shown only when not on the accept list — yellow "Wrong enchant (have X) → recommend Y", `shortEnchant` trims the "Enchant <Slot> - " prefix; **right-click the row to accept/un-accept** that enchant on the item via `ns.ToggleEnchantIgnore`, handled in `detail/Layout.lua`'s shared row `OnMouseUp`, which then re-runs `OnBeforeShow`+`Fit`; **accepting shows a confirm popup** (`StaticPopupDialogs.WARBANDEER_ACCEPT_ENCHANT`) since it silences the flag, while un-accepting is immediate; missing and wrong are mutually exclusive), and/or an **empty gem socket** (`ns.EmptySocketSlots`/`self._emptySockets`) each add a note — orange "Missing enchant" / yellow "Wrong enchant" / cyan "Empty socket" — with the recommendation appended ("→ Enchant Ring – …" via `ns.RecommendedEnchant`; "→ `<gem>`" via `ns.RecommendedGems`). The gem is split: the **unique diamond** (`self._gemPrimary`) goes on the *first* empty socket only (tracked by `self._gemPlaced` across the gear loop), every other socket gets the repeatable fill gem (`self._gemSecondary`). **Each present line (upgrade, enchant, socket) renders on its own pooled sub-line label** (`row.subs`, up to `GEAR_MAX_SUBS`=3, each anchored `GEAR_UP_H` below the previous; the row grows one `GEAR_UP_H` per shown line — a single multi-line label can't be used, `wordWrap=false` collapses `\n` to one truncated line). Each line is inline-coloured so the label's muted base doesn't override it (upgrade green/gold via `GREEN_CODE`/`GOLD_CODE`, notes orange). The name column autosizes to fit the wider of name / widest sub-line, and the upgrade's link is stashed on the row frame (`_upgradeLink`) so hovering shows it as a side-by-side comparison tooltip next to the equipped item, and **left-clicking the row flashes that upgrade item in the open bags/bank** (`ns.FlashItem`). Beneath the professions block in the left column sits the `ns.SuggestedBox` "SUGGESTED" panel (anchored where the next profession panel would go, width `PANEL_W`, height folded into `leftH` in `OnBeforeShow`) listing the character's immediately-equippable gear upgrades. Character-picker `BuildFilter`; `Select(toon)` switches subject; `OnNavigate()` resets to the logged-in character. **Live gear refresh:** `init.lua` registers `PLAYER_EQUIPMENT_CHANGED` and, when the Detail view is the shown view for the logged-in character, re-runs `OnBeforeShow` (+`MainWindow:Fit`) after a `DETAIL_REFRESH_DELAY` (750ms, outlasting the equipment broker's async rescan) debounce — so a suggestion clears the moment the gear changes. This matters most for the Suggested box's fixed vendor entries, which (unlike held/WQ ones) never drop out of an inventory cache and so are gated only by the equipped-ilvl comparison. `MainWindow:ShownView()` (window.lua) returns the visible view or nil so the refresher only fires when Detail is actually on screen. **The view lays out in three columns** — a left column (identity / stats / professions / Suggested → `leftH`), a **middle** column (the gear panel + Consumables → `col2H`), and a **third** column right of the gear panel (the appearance box + pet-roster button → `col3H`); the view sizes to `max(leftH, col2H, col3H)` and out to the third column's right edge, falling back to the two-column width for a class with no appearance box or roster (e.g. Evoker). Splitting the tall appearance block into its own column (rather than stacking it under the gear list) stops the window growing off the bottom while the left column sits half-empty. **The middle column carries `self.consumeBox`** (`ns.ConsumablesBox`), anchored beneath the gear panel in `OnBeforeShow` (`TopLeft → gearPanel BottomLeft`, width = gear panel width) and `Populate`d each render; its height (when shown) folds into `col2H`. It fills the previously-empty space below a short gear list with the spec's ClassCodex consumables. **The third column is headed by `self.glyphBox`** (`ns.GlyphBox`), anchored right of the gear panel at the stat-card band top (`col3X` = gear panel right edge + `GAP`, fixed width `D.APPEAR_W`) and `Populate`d each render; its height folds into `col3H`. It shows the character's applied cosmetic glyphs for a **spec-picker-selected** spec (a labeled list) + learned per-character class unlocks (a labeled list titled per class — Druid Tome of the Wilds / Hunter Tomes & Tames) + account-wide appearance unlocks (an icon grid; Druid/Warlock only) + the class's account-wide class mounts + spec tints (a labeled list; all classes but Evoker). Its `onSpecChange` is wired here to re-run `OnBeforeShow`+`Fit`, since switching spec changes the card height. A class with neither system (e.g. Evoker) hides the box. **Below the appearance box, Hunters and Warlocks get `self.petsButton`** (`ui.Button`, `HUNTER`-or-`WARLOCK`-gated) labelled `Pets — N` for Hunters (active+stable count) or `Demons — N` for Warlocks (summoned-demon count) that **toggles** the docked pet/demon roster panel (`self.petsPanel` = `ns.PetsPanel`, `controls/PetsPanel.lua`, lazily built by `_getPetsPanel` and anchored to the window's right edge; for a **Hunter** the button also opens the sibling `self.tamesPanel` = `ns.ChallengeTamesPanel` (`controls/ChallengeTamesPanel.lua`, `_getTamesPanel`, docked to the pets panel's right edge) in the same open/closed state — `PetsPanel:IsOpen()` keeps the two in sync); the button sits beneath the appearance box in the third column (its extent folds into `col3H`), and it's hidden (zero extent) for classes with no pet roster. `OnBeforeShow` calls `petsPanel:Refresh(char)` (and `tamesPanel:Refresh(char)` for a Hunter, hiding the tames panel for a Warlock) so open panels re-point at a newly-selected subject, and hides the button + both panels for classes with no roster |
| `views/detail/Layout.lua` | `ns.detail` — shared Detail layout metrics (`P`/`GAP`/`PANEL_W`/`GEAR_*`/`PG_*`/header-icon sizes …) + leaf helpers (`attachItemTip`, `rarityColor`, `reqLevel`, `tierAtlas`, `intentColor`, `raceAtlas`, `INTENT_OPTIONS`, `CHEVRON`, `gearInnerW`/`gearPanelW`). Loaded before `DetailView.lua`; the other detail files alias `local D = ns.detail` |
| `views/detail/Professions.lua` | DetailView profession-row pool: `_profRow` (icon + skill `LabeledBar` + intent `FilterDropdown`), `_profGearRow` (equipped tool/accessory sub-rows), `_showProf` |
| `views/detail/Gear.lua` | DetailView equipped-gear row pool: `_gearRow` (slot icon + name + ilvl + track + stacked sub-lines), `_showGear`. Each row also carries two pooled transparent mouse-catchers (`row.enchHover` / `row.gemHover`, built by `noteHover`, parked by `parkHover`) repositioned over the **missing-enchant** / **empty-socket** sub-lines each render (a slot may have both); hovering one shows that recommendation's own tooltip in place of the row's equipped-item tooltip — `ns.ShowEnchantTooltip` (enchant effect, raw `ns.RecommendedEnchantInfo`) / `ns.ShowGemTooltip` (gem stat, raw `ns.RecommendedGemsInfo`) |
| `views/detail/Stats.lua` | DetailView secondary-stat 2×2 grid (Crit + Mastery / Haste + Versatility) under the stat cards: `_buildStatGrid` (lazy, 4 module-bg cells — two narrowed columns with `ns.StatChart` filling the centre channel between them, sized `D.STATS_CHART`) + `_showStats` (fills `pct`/`rating` from the data layer's `stats.secondary`, blanks `—` when unscanned, and feeds the four gear ratings to the centre radar via `StatChart:Set`). The spec's tier-1 stat(s) are tinted gold via `ShadowsOfUI_UpgradeApi:StatRanks` (OptionalDep). When `:StatTargets` returns an Archon target the rating shows as "current / target" with the current `deltaState`-coloured (±5% green = on-target, blue = over, red = under); each cell is hover-enabled (`showStatTip`) and shows a colour-matched status tooltip ("In/Over/Under target" + signed delta + current/target) when it has a target. A child hit-frame over just the percentage value (`showPctTip`, tracking the label as its text resizes) shows a separate plain-language tooltip explaining what that percent means in game (`EXPLAIN` per stat) — it captures the hover in place of the cell's target tooltip over the number, leaving the rest of the cell on the target tip. The **mastery** cell overrides the generic note with its **spec-specific** mastery (`masteryOverride` → "Mastery: \<name\>" + the passive's own description, resolved live from the captured `stats.secondary.mastery.spell` id), falling back to the generic note for characters not yet re-scanned with the spell id. **Secret values:** a rating/percent read back from the DB is passed through `safeNum` (`canaccessvalue` guard) and treated as nil if "secret" — so a value persisted before the data layer began filtering them can't taint the delta/`StatChart:Set` arithmetic (shows `—` / blank / radar 0 instead) |
| `views/detail/Filter.lua` | DetailView `BuildFilter` (titlebar character picker) + `sortedCharacters` |
| `views/RoleView.lua` | `ClassTable` per class, grouped by spec |
| `views/RaceView.lua` | 13×29 class/race grid (dynamic build), one character per cell; hover + click-to-Detail |
| `views/Legion.lua` | Hidden artifact appearances + Legion achievements |
| `views/Milestones.lua` | Collectible-reward achievement grid (mounts/toys/decor) spanning all expansions — **not** Midnight-specific (renamed from `Midnight.lua`) |
| `views/ProfsView.lua` (+ `views/profs/ProfsData.lua`) | Best-skill-per-expansion grid + per-character detail panel. The view shell (`ProfsView` class + its methods) is in `ProfsView.lua`; the shared column factories, gradient `skillCell`, and per-expansion skill aggregation (`buildBestSkills`/`buildCharList`) + the expansion/profession order tables live in `profs/ProfsData.lua` on `ns.profs` (loaded first). **Sortable char list (#422):** `makeCharColInfo` marks the CHARACTER column `sortable`/`sortKey="name"` and each expansion column `sortable`/`descFirst=true` (icon col not sortable); `charTable.onSort` → `ProfsView:_sortCharList` re-sorts the stored `_charEntries` via `ns.profs.sortCharEntries` (expansion skill with **missing = `-math.huge`**, name-asc tiebreak) and repaints through the extracted `_renderCharRows` (which rebuilds the index-parallel `_charToons` + per-row click closures together, so a row click still opens the right toon). `RebuildCharList` re-applies the table's active `Sort()` so a chosen sort persists across profession switches. A character with no captured per-expansion skill — `professions.details[skillID]` is nil (window never opened) **or** present but with an empty `expansions` list (a `TRADE_SKILL_SHOW` scan that fired before the child skill-lines loaded writes recipes/specPoints but no expansions, and that sticks) — renders an all-"—" row, so it gets a caution glyph (`ns.icons.Warning`) prefixed to its name + a tooltip hint to open that profession on that character. Condition `not (detail and detail.expansions and #detail.expansions > 0)`; every profession here (incl. Cooking/Fishing) carries expansion data, so an empty list is always a capture gap. Mirrors `missing.lua`'s `professions (X)` report (same broadened condition) |
| `views/MidnightProfs.lua` | Profs × characters grid: Midnight skill + concentration + recipes%, plus a per-character "Know" column (weekly profession knowledge collected/available, via `WarbandeerApi:GetProfessionKnowledge`) |
| `views/CraftingView.lua` | Crafting profs: main crafter, concentration, learned-recipe %; expansion `BuildFilter` |
| `views/PlaytimeView.lua` | Per-character playtime breakdown: total `/played` + **Today** and **7 days** columns summed from `playtime.byDay` (`recentSum` walks local-day keys back from `GetServerTime`; per-day cells blank at 0). Warband total below |
| `views/BarsView.lua` | Action-bar profile browser (class/spec filters + result list); needs `WarbandeerBarsApi` (OptionalDep) |
| `views/BarsPreview.lua` | `ns.BarsPreviewFrame` — companion box docked right of the window that hosts LibNUI's shared **`ui.BarsPreview`** render widget (the real-layout 3-area render, stance model, gold outline, stacked vertical labels — all moved to LibNUI so ABM can share it, nazumods/wow#466 follow-up). The widget now lays normal bars out as a **condensed map of their real on-screen topography** (coordinate-compressed screen X/Y from `barLayout`), with stance/Sky pages + pet in the gold-outlined group. This file now owns only Warbandeer's **profile-schema slot resolvers** (`slotTex`/`slotName`/`petTex`/`petName` — passed to the widget as `resolveIcon`/`resolveName`/`resolvePetIcon`/`resolvePetName`), the Edit-Mode `editLayoutFor` orientation fallback (`WarbandeerBarsApi:GetLayout`), the theme colours passed in (`rowColor`=module, `highlightColor`=selected, `stanceColor`=gold), and the companion box itself: the `<char> — <spec>` heading + the **header drag** (`_dragStrip` wired once in `Set()` via `ns.MainWindow:BindDragHandle`, #420). `Set(profile)` forwards to the widget; `HighlightProfileBar(profileBar, on)` forwards to `ui.BarsPreview:HighlightBar` (apply-panel toggle hover). The widget consumes the profile's captured `barLayout` (real per-character geometry from `ns.wow.ReadActionBars`) — see LibNUI's `BarsPreview.lua` for the render/stance detail |
| `views/BarsApply.lua` | `ns.BarsApplyFrame` — companion box docked below the preview box (the stable right column; anchored to the preview's bottom-left so it tracks the preview height, keeping the fixed-size panel clear of the dynamic character list): per-bar include toggles (1-8 / C1-C5 / Bonus, Sky, Pet) + Apply button → `WarbandeerBarsApi:Restore` with a `barFilter`. **Interaction model (#365):** a 2-line header (`APPLY TO <char>` + an `OVERWRITES <char>'S BARS` warning), an **included (gold) / excluded (red)** swatch legend, each toggle painted as a gold-fill+gold-accent (included) or empty+red-accent (excluded) square with a state-aware hover lift, a footnote `Keybindings and outfits are never changed.`, and a primary Apply button (`ui.Button` `glow`, `APPLY N BARS` live count, `ConfirmFlash("APPLIED")` in-panel confirmation). Toggle **hover → `_previewFrame:HighlightProfileBar`** lights the matching preview bar (`_previewFrame` handle set by `BarsView:_showSelection`), making the profile-bar↔preview-sysIdx mapping visible. Each toggle's initial include-state is read at construction via `defaultOn(def)` — `db.settings.barApplyDefaults[label]` if set, else the built-in `not def.off`. Exposes its `TOGGLES` list as `ns.BarApplyToggles` for the settings file |
| `views/barApplyDefaultsSettings.lua` | Builds the **Bar Apply Defaults** settings subcategory (nested under "Warbandeer"): one checkbox per `ns.BarApplyToggles` entry, default `not def.off`, writing `db.settings.barApplyDefaults[label]` (the `table` closure ensure-creates it). No live callback — read when the apply panel is next constructed. Friendlier labels than the terse panel captions (Action Bar N / Class Page N / Bonus·Skyriding·Pet Bar). Loads after `BarsApply.lua` so `ns.BarApplyToggles` exists |
| `views/CollectedView.lua` | Warbandeer's **embedded host** for the shared transmog-set collection grid. **The grid itself — its cells, rating marks, PTR logic, `embedded` mode, `BuildColInfo`, and the `infoTipAnchor`/`onWantedToggle`/`onResized`/`onEnsureVisible` host hooks — is `Warbandeer_Collected`'s `DataView`, maintained and documented in that addon (`/collected`); see its CONTEXT for the grid.** This view owns only the Warbandeer-side chrome + lifecycle: it builds the grid via `WarbandeerCollectedApi.DataView:new{ embedded = true, colInfo = …BuildColInfo(true), infoTipAnchor = ns.InfoTipPosition, onWantedToggle = function() _view:RefreshWanted() end, onResized = function() _view:_fitToGrid() end }` (embedded → no lock column/lockout panel; those stay in `/collected`'s own window) inside a capped `ScrollFrame`; the mode counter + a gold **wanted tally** (`wantedCount`/`RefreshWanted`, `WantedIcon` atlas + `WarbandeerCollectedApi:WantedCount()`) over the grid header; and a **filter strip** above the grid (`filterStrip`) built by the shared `grid:BuildFilterStrip(self, onModeChanged)` — PTR / Wanted / Sort toggles + Expansion / Category `ui.FilterDropdown`s, identical to the `/collected` window since the chrome lives on `DataView` now (the grid is offset down by `STRIP_H`+gap; `onModeChanged` re-renders so the counter tracks the PTR toggle). `_fitToGrid()` caps the grid at `DataView.MAX_HEIGHT`, sizes the `ScrollFrame` + view height to the (filtered) row area, and calls `scroll:Refresh()` — run at construction and again via the grid's `onResized` hook on every filter/PTR change, so the scroll range tracks the visible rows and can't overscroll (issue #245). `OnBeforeShow`→`_render` sets the counter (`Sets: N / M`, or `+K sets upcoming · PTR <build>` from `WarbandeerCollectedApi.PtrBuild` in PTR mode) and the pre-scan empty state (`_showEmpty`/`_showGrid`). **Registration gating:** added to `ns.views` only when the Collected addon is loaded (OptionalDeps load first, so the API global is already set), so without it there is no nav icon, minimap-menu entry, or `/wb collected` command — all three derive from `ns.views` (the hardcoded `"collected"` slot in `ns.viewOrder` is harmlessly skipped by `NavViews`' `if not c` guard). Live-refreshed in sync with the `/collected` window via `init.lua`'s `refreshCollectedOnScan` (`WarbandeerCollectedApi:OnScanned`) and a module-level `WarbandeerCollectedApi:OnRatingsChanged` listener; a module-level `WarbandeerCollectedApi:OnDressedSetChanged` listener draws the cell cursor on the set shown in the shared dressing room (#396) |
| `views/HousingDecorView.lua` | Warbandeer's **embedded host** for the shared housing-decor list. **The list itself — its rows, filter strip, and wanted logic — is `Warbandeer_HousingDecor`'s `HousingDecorList` (a windowed scroll list), maintained in that addon (`/housingdecor`); see its CONTEXT for the list.** This view owns only the Warbandeer-side chrome: it builds the grid via `WarbandeerHousingDecorApi.List:new{ embedded = true, onFilterChanged = function() _view:RefreshCounter() end }` at a fixed `CONTENT_W`×`LIST_H` (the windowed list scrolls internally, so no `_fitToGrid`), a `filterStrip` above it (`grid:BuildFilterStrip(self)` — Unowned + Wanted toggles + Category dropdown + search), and a counter + gold **wanted tally** (`RefreshWanted`, `WarbandeerHousingDecorApi:WantedCount()`). `OnBeforeShow`→`RefreshCounter`/`RefreshWanted`; before the first scan the grid is hidden behind an `emptyMsg`. **Registration gating:** added to `ns.views` only when the Housing Decor addon is loaded (so without it there is no nav icon, minimap-menu entry, or `/wb decor` — all derive from `ns.views`; the `"decor"` slot in `ns.viewOrder` is harmlessly skipped). Live-refreshed via module-level `WarbandeerHousingDecorApi:OnScanned` + `:OnRatingsChanged` listeners, cross-updating with the standalone `/housingdecor` window |
| `views/ReputationsView.lua` (+ `views/reps/`) | **Split across files**, sharing constants + helpers via `ns.reps` (loaded first): `reps/ReputationsData.lua` (layout constants, the `categoryId`→release map + fallback names, and the inline-colour cell helpers `classCode`/`cleanLabel`/`standingText`/`factionIconSpec`/`applyIcon`), `reps/ReputationsRender.lua` (the `ui.VirtualList` row builders — `_createRow`/`_updateRow`/`_renderPage`/`_highlight`, reopening the class). The `ReputationsView.lua` shell holds the class + data (`_refreshData`/`_pageOptions`), selection/navigation (`_select`/`_flipPage`/`_onKey`), `BuildFilter`/`OnBeforeShow`, and the `/wb reps0` dev command. Paged cross-alt reputation browser. One page **per expansion** (plus a trailing **Other** page for guild/uncategorized), only pages with ≥1 renowned faction across the warband. Page switcher is a titlebar `ui.FilterDropdown` (LibNUI) whose option labels inline the Collected addon's **expansion badge** (`WarbandeerCollectedApi.Releases`+`.ReleaseIcons`, OptionalDep — text-only when absent) + the **Left/Right arrow keys**. Body is a `ui.VirtualList` (#409) of faction rows (spacing/padding 0, pixel-flush): faction emblem (major texture-kit / friendship / generic fallback via `factionIconSpec`) + name + an **Alliance/Horde marker** when the rep is side-locked (`ns.factionIcon`, inferred from which sides hold it — only flagged when the opposite side has a scanned character) + the **highest standing across the warband** (`GetFactionStandings(fid)[1]`, green at cap / blue paragon). **Up/Down arrows** move the row selection (highlight + scroll-into-view via `VirtualList:Row`+`:ScrollToItem`); selecting/hovering a row shows every character's standing in `ui.tip` (class-coloured, capped at 8 + "+N more — /sreps"). Grouping keys off the reputations broker's per-faction `categoryId` → release via a static map. Arrow capture is scoped to while the view is shown (`EnableKeyboard` on show/hide + `SetPropagateKeyboardInput` so non-arrow keys pass through). Pages rebuilt each `OnBeforeShow` from `WarbandeerApi:GetReputations` across all characters |
| `views/TitlesView.lua` (+ `views/titles/`) | **Split across files**: `titles/TitlesCompute.lua` — the **pure** `ns.ComputeTitles(universe, characters, epithetById)` → `{ list, counts }` categoriser (no WoW API, busted-tested); `titles/TitlesData.lua` — layout constants + the impure shell on `ns.titles` (`scanUniverse` walks `1..GetNumTitles()` keeping `GetTitleName`'s `playerTitle`-flagged mask ids, **without** gating on `IsTitleKnown` — it records that per entry as `current` (the account-wide oracle) instead — so it yields the full earned+unearned universe; `gatherCharacters` maps `WarbandeerApi.GetAllCharacters` → `{name,classKey,known=c.titles.known}`; `epithetById` re-keys the optional `_G.EpithetData.titles` SavedVar by `titleID` (nil when Epithet absent/empty); `rarityCode(ep)` → `ITEM_QUALITY_COLORS[ep.q]` escape); `titles/TitlesRender.lua` — the `ui.VirtualList` row builders `_createRow`/`_updateRow`/`_renderPage`/`_highlight` (reopening the class). The `TitlesView.lua` shell holds the class + `_refreshData` (scan → `ns.ComputeTitles` → pre-binned per-status lists), `_statusOptions`/`_currentList`, selection/navigation (`_select`/`_flipStatus`/`_onKey`), `BuildFilter`/`OnBeforeShow`. Warband **Titles browser** (`/wb titles`). One `ui.VirtualList` of every player title the client knows (earned + unearned); a titlebar `ui.FilterDropdown` pages by status — **All / Earned / Unearned** (+ **Earnable** / **Unearnable** with Epithet) — synced to the **Left/Right arrow keys**; **Up/Down** move the row selection. "Earned" is the **account union** (a title any tracked character has, from each `Character.titles.known`): earned rows get a green check, unearned rows are muted, and selecting/hovering shows in `ui.tip` either **"Earned by Warband"** for account-wide titles or the per-character **owner breakdown** (class-coloured via `ns.Colors.className`, capped at 12) for character-specific ones. Account-wide vs character-specific keys off `universe[i].current` (the logged-in char's live `IsTitleKnown`): account-wide titles propagate to every character, so a title the current char knows is warband-wide, while one only an alt holds is that alt's — a title also counts as earned when `current` is true even if no stored alt lists it yet. **Optional Epithet enrichment** (`## OptionalDeps: Epithet`, read **live** — never bundled, no license): rarity-coloured earned names, a Source/Category tooltip line, and the derived **Earnable** = unearned ∩ Epithet `obtainable=="yes"` and **Unearnable** = the remaining unearned (`obtainable` != yes / unknown). No bundled data (the universe is the live scan); degrades to earned/unearned without Epithet. Pure core spec: `Warbandeer/spec/{warbandeer,titles_spec}.lua`, wired into `.busted` |
| `window.lua` | `MainWindow` (TitleFrame) + `IconStrip` rail; `ns:Open()`, `ns:Toggle()` (close if shown, else open — used by the keybinding), `ns:view(name)`, `ns.NavViews()` (deterministic rail order, shared with the minimap menu); `Fit()` grows the window down/right. `BindDragHandle(frame)` wires an external LibNUI frame as an extra drag grip (move + `SavePosition` on release; used by the Bars preview heading, #420). `ns.RefreshSummaryColumns()` forgets+rebuilds the Summary view (re-showing it if visible) when a column show/hide setting changes; `ns.RefreshVaultColumns()` does the same for the Great Vault view |
| `commands.lua` | Registers the base open command + one per view (from `ns.views`), plus `enchants` (list accepted wrong-enchants from `WarbandeerDB.ignoredEnchants`, resolving item names) and `enchants clear` (wipe the accept list + re-render the open Detail view) |
| `minimap.lua` | Minimap button via `ui.MinimapButton` (LibNUI): left-click toggles the window, drag to move; right-click menu = Hide minimap button + the shared **view menu** (`ns.populateViewMenu`: Settings via `Settings.OpenToCategory(ns.settingsCategory)`, divider, then a row per nav view (`ns.NavViews()` → `ns:view`), each prefixed with the view's rail glyph via an inline `|T icons/views/<name>.tga |t` — no rotation, so Bars shows upright). `populateViewMenu` is exposed on `ns` so the minimap button, the addon-compartment entry, **and the nav rail's brand-mark logo** all share one Settings + views menu. State in `WarbandeerDB.minimap` (lazy-init, no DB bump); `ns.SetMinimapShown`, `/wb minimap`. Also surfaces a **Hide minimap button** checkbox in the Warbandeer settings panel via a second `ns:RegisterSettings` here (reusing the "Warbandeer" category, like `summaryColumnsSettings.lua`) — bound directly to `minimap.hide` (checked = hidden), its `table` closure ensure-creates `db.minimap` so the field isn't skipped at `ADDON_LOADED` (the button's own lazy-init only runs at `PLAYER_LOGIN`), and its `callback` calls `ns.SetMinimapShown` for live apply; the slash command, the right-click menu, and this checkbox all drive the same `minimap.hide`. The compartment entry comes from the `.toc` `AddonCompartmentFunc`, so the widget's `compartment` option is omitted; its `ns:CompartmentClick(btn)` handler also lives here (not window.lua) — **right-click opens the same shared view menu** (`MenuUtil.CreateContextMenu` owned by the always-present `AddonCompartmentFrame`, pops at cursor; no "Hide minimap button" row), any other click opens the window |
| `keybind.lua` + `Bindings.xml` | `WARBANDEER_TOGGLE` binding (category "Warbandeer") shown in Esc → Key Bindings for the user to assign (recommended Alt-W). **`Bindings.xml` is auto-loaded by the client from the addon root — never list it in the `.toc`** (doing so routes it through the generic UI-XML loader → "Unrecognized XML" warnings + an orphaned `HEADER_*` row). New keybindings UI groups by the `category` attribute, not legacy `header=`. `runOnUp="true"`, so the global `Warbandeer_ToggleBinding(keystate)` fires on both edges → dispatches to `ns:RingKeyDown/Up` (ring.lua). No forced default key (would need persisted state to avoid re-grabbing the key each login) and no DB state — pure insecure UI, taint-free |
| `ring.lua` | Hold-to-open radial view selector. A **tap** of the toggle key → `ns:Toggle()`; **holding** past `HOLD_THRESHOLD` (0.2s, a `C_Timer`) pops a `LibNUI` `RingSelector` (`ui.RingSelector`) under the cursor, laid out from `ns.viewOrder` reusing each view class's glyph (`icons/views/<name>.tga`) + `_title` + `_iconRotation` — the same metadata the IconStrip rail reads. Release commits the aimed view via `ns:view(name)` (dead-zone aim = cancel). `ns:RingKeyDown/Up` |

## Views

| name | _title | Parent | Key feature | `BuildFilter` |
|---|---|---|---|---|
| `overview` | Overview | Frame | Stat strip, reputations, achievements, top characters (per-raid RF/N/H/M set completion; set cells carry wanted-star + tier-letter overlays, Shift-click toggles wanted, live-refreshed on dressing-room rating edits — like the Collected view). Expansion tabs Midnight / The War Within / Dragonflight (`expansionLevel` 11/10/9) | raid + expansion dropdowns |
| `summary` | Summary | Frame | Three ClassSummary rosters (Alliance / Horde / Both-merged) | 3-way faction segmented control |
| `vault` | Great Vault | Frame | Three `VaultRoster` rosters (Alliance / Horde / Both-merged) of each character's Great Vault: per-track (Raid / M+ / World) slot **pips** (filled = unlocked, hollow = locked; per-slot reward ilvl in tooltip), owned keystone, primary raid lockout (tooltip lists all), Delver's Bounty, an unclaimed-vault marker + a footer tally. Reads `weeklies.vaultSlots`/`vault`/`keystone`/`dungeons`/`delversBounty` + `WarbandeerApi:GetRaidLocks`; own segmented faction control (Summary untouched) | 3-way faction segmented control |
| `detail` | Detail | Frame | Per-character detail + profession-intent editor + per-profession gear list | character picker |
| `gear` | Gear | Frame | 4 armor-type tables, per-slot columns | armor-type buttons |
| `roles` | Roles | Frame | ClassTable per class, grouped by spec | — |
| `races` | Races | TableFrame | 13×29 grid, one character per cell | — |
| `profs` | Professions | Frame | Profession skill grid + detail panel | — |
| `crafting` | Crafting | Frame | Main crafter, concentration, recipe % | expansion dropdown |
| `milestones` | Milestones | Frame | Collectible-reward achievement grid | — |
| `legion` | Legion | Frame | Hidden artifacts + achievements | — |
| `playtime` | Playtime | Frame | Per-character playtime — total + Today / 7-day columns (`playtime.byDay`) | — |
| `midnightprofs` | Midnight Profs | Frame | Profs × characters: skill + concentration + recipes% + weekly "Know" column | — |
| `bars` | Bars | Frame | Bar-profile browser + docked preview & apply panels | — (view-local dropdowns) |
| `collected` | Collected | Frame | The shared `/collected` transmog-set grid (`Warbandeer_Collected`'s `DataView`) embedded in Warbandeer — see that addon's CONTEXT for the grid; this view is the host chrome (counter, wanted tally, toggles) and is only registered when the Collected addon is loaded | PTR + wanted-only + raid-order toggles (`BuildFilter`) |
| `decor` | Housing Decor | Frame | The shared `/housingdecor` housing-decor list (`Warbandeer_HousingDecor`'s windowed `HousingDecorList`) embedded in Warbandeer — see that addon's CONTEXT for the list; this view is the host chrome (counter, wanted tally) and is only registered when the Housing Decor addon is loaded | Unowned + Wanted + Category + search (in-view strip) |
| `reputations` | Reputations | Frame | Paged cross-alt faction browser (one page per expansion + Other); rows = faction + highest warband standing + side marker, hover shows per-character standings; Up/Down select faction, Left/Right flip pages | expansion picker (badge-labelled) |
| `titles` | Titles | Frame | Warband title browser — one `ui.VirtualList` of every player title (earned account-union + unearned) from the live client scan; green check = earned, muted = unearned, hover shows the owning characters; status pulldown All/Earned/Unearned (+ Earnable/Unearnable with Epithet), Up/Down select, Left/Right flip status. Optional **Epithet** read (live) adds rarity colour + source + the Earnable list | status picker |

`BuildFilter(parent)` widgets show in the title bar only while that view is active. The two
expansion dropdowns + the Overview raid picker share `ui.FilterDropdown` (LibNUI) (Overview returns a
container holding both its raid and expansion pickers); the faction segmented control, character picker,
and armor-type strip are view-local.

## MainWindow

Subclasses `TitleFrame`; `special=true`, `level=600`, `theme = ns.theme` (inherited by all window widgets; table headers default to the theme's muted `header` token, so views no longer set header colors explicitly).
- **Views are constructed lazily**: `MainWindow:getView(name)` builds a view (and wires its `BuildFilter`) on first navigation; only the default view is built when the window opens. The nav rail therefore reads **class-level** metadata — every view class sets `name`, `_title` (and optionally `_iconRotation`) as class fields after `Class(...)`, not (only) in defaults. Row-click cross-navigation must go through `w:getView("detail")`, never `w.views.detail` (may not exist yet).
- `self.iconStrip` — `IconStrip` rail docked just left of the window, one glyph per view in `ns.viewOrder` order (unlisted views appended, sorted by title). Clicking a glyph calls the view's `OnNavigate()` (if defined) then `self:view(name)`.
- `MainWindow:view(name)` — hides current, shows named (building it if needed), updates title+size, calls `iconStrip:SetActive(name)`.
- Window is anchored by a single **TOPLEFT** point (stored in `db.settings.windowPos`) so view changes grow it down/right instead of re-centering. `SavePosition`/`RestorePosition` persist/apply it; titlebar drag saves on release. `MainWindow:BindDragHandle(frame)` wires an external LibNUI frame as a second drag grip (press-drag → move + `SavePosition`), keeping the window's `_widget` encapsulated — used by the Bars preview heading (#420).
- `ns:Open()` lazy-creates the window; `ns:view(name)` = `Open()` then `OnNavigate()` (if the view defines it) then `view(name)`. Both the icon rail and slash commands count as *direct navigation* and fire `OnNavigate`; row-click paths (`w:getView("detail"):Select(toon)` + `w:view("detail")`) call `MainWindow:view` directly and skip it, so the clicked character stays selected.

## Overview — Factions Widget

`FactionBars` (Frame subclass, in `views/overview/FactionBars.lua`, registered on `ns.overview`)
stacks one `LabeledBar` per major faction plus optional subfaction rows; data built by file-local
`gatherFactions`. Bar **fill = faction colour**, resolved by
`colorFor(id, apiColor, fallback)`: `ns.data.factionColors[id]` override → API `factionFontColor` →
fallback (subfaction falls back to parent). Maxed factions with paragon unlocked show **paragon
progress on a darker-faction-colour track**; the value reads green **"paragon"**, swapping to raw
`prog / threshold` on row hover (`LabeledBar.hoverValue`). A faction with a **claimable paragon
reward** (`hasRewardPending`, the 4th return of `GetFactionParagonInfo` — live logged-in character
only) instead reads gold **"claim"** beside an animated **reward bag** (`ns.overview.ParagonBag` —
Blizzard `ParagonReputation_Bag` + `ParagonReputation_Glow` atlases with a looping scale/alpha
pulse), carried through `paragonInfo`→`resolveProgress` as `reward` onto the row fragment (major
factions, subfactions, and `extraFactionIDs` alike). Each `FactionBars` tallies `self.rewardCount`;
`Overview:BuildFilter` sums it across expansions for a titlebar **"N to claim" badge** (shown only
when >0, pulsing bag + count) and leading-`ParagonReputation_Bag`-marks the expansion-picker
dropdown options that hold a claim. Counts are computed at panel build (refresh on reopen).

**Seasonal-rep grouping:** a **seasonal reputation** names itself `"<type>: Season N"` (e.g.
`Delves: Season 1`). `gatherFactions` tags each row's `season` (`seasonOf` → `name:match("Season (%d+)")`,
nil = evergreen) and, before returning, moves all seasonal rows to the **bottom** grouped by
`seasonKind` (the label before `": Season"`, in first-appearance order) and sorted by season within
each group — so e.g. every `Delves: Season N` sits together S1→S2→S3, then every `Prey: Season N`,
below the evergreen reps. No filtering; all rows always show.

**Constructor options:** `expansionLevel` (9=Dragonflight, 10=TWW, 11=Midnight; passed to `GetMajorFactionIDs`),
`extraFactionIDs` (IDs the API omits, e.g. Silvermoon Court `2710`, Slayer's Duellum `2770`), `width`.

**Subfaction tiers** (tried in order): (1) `GetMajorFactionData` → renown; (2)
`GetFriendshipReputation` with `maxLevel > 1` → friendship rep (e.g. Valeera `2744`); (3)
`C_Reputation.GetFactionDataByID` → standard standing / `minorFactionMaxStanding[parentID]`.

**Renown bar fill:** a non-maxed renown faction (major or subfaction) fills by **within-rank
progress toward the next rank** (`renownReputationEarned / renownLevelThreshold`), *not* the rank
count out of max — the rank stays in the value text (`level / max`) with the raw XP revealed on
hover (`LabeledBar.hoverValue`). Friendship/standard subfactions fill by within-tier standing.

## SavedVariables (`WarbandeerDB`)

```lua
{ version = 8,
  settings = { defaultView = int, tooltipSide = int, windowPos = { x, y },
               -- per-column Summary visibility; [columnKey] = false hides it
               -- (absent/true = shown, so {} means every column visible)
               summaryColumns = { [columnKey] = bool },
               -- per-column Great Vault visibility; same shape/semantics as summaryColumns
               -- but a separate registry ({} = every Vault column visible)
               vaultColumns = { [columnKey] = bool },
               -- master toggle for the Detail Consumables box; false hides it entirely
               -- (absent/true = shown, so no migration needed)
               showConsumables = bool,
               -- per-category Detail Consumables-box visibility; [catKey] = false hides it
               -- (catKey ∈ flask/combatPotion/food/weaponBuff/augmentRune; {} = all visible)
               consumables = { [catKey] = bool },
               -- default include-state per Bars Apply toggle; [label] = false starts it
               -- unchecked (label ∈ 1-8/C1-C5/Bonus/Sky/Pet; absent = built-in default,
               -- i.e. all on except Bonus/Sky/Pet, so {} changes nothing)
               barApplyDefaults = { [label] = bool } },
  -- per-character, per-profession crafting intent: "main" | "secondary" | "gatherer"
  profIntent = { [charName] = { [skillLineID] = intent } },
  -- per-item accepted wrong-enchants; key "<itemID>:<enchantID>" = true means the
  -- (non-recommended) enchant on that specific item is fine — Detail won't flag it
  ignoredEnchants = { ["<itemID>:<enchantID>"] = true } }
```
`MigrateDB`: v1 seeds `settings.defaultView`; v2 adds `profIntent = {}`; v3 seeds `tooltipSide = 1`
(Left); v4 seeds `settings.summaryColumns = {}` (every column visible by default); v5 seeds
`ignoredEnchants = {}`; v6 seeds `settings.consumables = {}` (every consumable category visible
by default); v7 seeds `settings.barApplyDefaults = {}` (every Bars Apply toggle at its built-in
default); v8 seeds `settings.vaultColumns = {}` (every Great Vault column visible by default).
All migrations are
non-destructive. `windowPos` is not migrated — written lazily at runtime.

## Gotchas

- **Cold-session FontString rasterization glitch.** On the first UI load of a client session (not after `/reload`), cell FontStrings in a TableFrame can render blank even though text/size/visibility/font all report correct via the API. A same-params `SetFont`/`SetText` is a client no-op and does NOT heal it; only a *real* font change does. Fix: the shared `ns.HealCellFonts(tbl)` (`init.lua`) swaps each cell label's font away (FrizQT) and back; every view holding a TableFrame calls it one tick after construction (`ns:after(50, …)`). Applied in **Overview** (`topAlts` + each panel's `_ach`) and **SummaryView** (`alliance`/`horde`); reuse it for any new TableFrame view that shows the same symptom.

- **Don't pass `ns.theme` to free-floating CleanFrames** (e.g. `CharacterTooltip`, parented to UIParent). The theme's `window` token is **alpha 0** — views layer it over the MainWindow surface — so a standalone frame using it as its CleanFrame background turns transparent. MainWindow itself keeps an explicit opaque `background` for the same reason.
- **Known-zero vs doesn't-apply in Summary cells.** Weekly currency columns (crests, field accolades, M+, caches, coffer keys) return the shared muted-em-dash cell `ns.ZeroDash` for a **max-level** character that genuinely holds 0, distinguishing it from a **below-max** character the column doesn't apply to (which stays empty `""`). The gate is `t.basic.level >= ns.wow.maxLevel`. The upgrades column returns `ns.ZeroDash` ungated for its flat n/a (no upgrades) case. `ns.ZeroDash` is a shared constant — safe because `SummaryView:decorateRow` copies every cell before wrapping.
- **Decorated table cells must be shallow-copied.** Several `getData` fns return shared table objects (e.g. `faction.lua` → `ns.icons.AllianceLight`); decorating in place chains hover/click wrappers across every row sharing the object and corrupts it globally. SummaryView/GearView/RaceView copy each cell before wrapping.
- **Row hover/click is cell-driven, not row-driven.** Data cells are mouse-interactive for their per-column tooltips, so a mouse-enabled row would steal those events. `decorateRow` chains onto each cell's `onEnter`/`onLeave`/`onClick`.
- **`LabeledBar` frame height comes from the body font size, not `nameLabel:GetHeight()`** — a FontString reports `GetHeight()==0` until laid out a frame later, leaving the frame too short to cover/receive mouse over the bar.
- **`LabeledBar` hover lightens its own fill+track** in addition to the row `highlight` backing, because the opaque bar (a child frame) occludes the highlight overlay.
- **Faction "done" ≠ `IsFactionParagon`.** Paragon caches exist from the start of Midnight factions. Use `renownLevel == maxLevel` (major) / `reaction >= 8` (standard); only consult paragon *after* max. `GetMajorFactionData`/`GetMajorFactionRenownInfo` return nil for Valeera (2744) despite `IsMajorFaction` — use `GetFriendshipReputation`. Some standard reps (e.g. Slayer's 2770) expose a dummy `friendshipFactionID` with `maxLevel == 1` — guard with `maxLevel > 1`.
- **Dynamic tables need explicit final width/height** — `addCol`/`addRow` accumulate only column/row spans, not header offsets (RaceView, RoleView).
- **Icon TGAs are white and tinted in-game**, rasterized from `@material-symbols/svg-400` (32-bit, descriptor 0x08, matching `bar-rounded.tga`) — see the design repo `iconbuild/`. Regenerate there to add view glyphs.
